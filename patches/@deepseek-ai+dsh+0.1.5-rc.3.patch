diff --git a/node_modules/@deepseek-ai/dsh/lib/profile-boot-Dk-7KqJc.js b/node_modules/@deepseek-ai/dsh/lib/profile-boot-Dk-7KqJc.js
index 5b8bea6..72ee860 100644
--- a/node_modules/@deepseek-ai/dsh/lib/profile-boot-Dk-7KqJc.js
+++ b/node_modules/@deepseek-ai/dsh/lib/profile-boot-Dk-7KqJc.js
@@ -1,5 +1,5 @@
 import { existsSync, mkdirSync, rmSync, writeFileSync } from "node:fs";
-import { fileURLToPath } from "node:url";
+import { fileURLToPath, pathToFileURL } from "node:url";
 import { PROFILE_PATCH_FILENAME, PROFILE_TEMPLATES, boot, composeEntries, healProfilesModuleFallback, initProfile, installFailLoud, loadOptionalPatches, loadOverlayPatches, loadProfile, resolveProfileDir, watchUserPatches } from "@deepseek-ai/dsh-app-boot";
 import { dirname, join, resolve } from "node:path";
 import { resolveDshHome } from "@deepseek-ai/dsh-home-paths";
@@ -129,6 +129,15 @@ const PROFILE_ROOT_CONFIG = `# dsh profile root — an empty entry list. The tre
 /** Root config filename inside a profile directory. */
 const PROFILE_ROOT_FILENAME = "cordis.yml";
 /**
+* DSH Desktop sets this for a profile whose every plugin ships with this
+* installation (Safe Mode). Bare plugin names then resolve from the
+* installation itself instead of through `$DSH_HOME/profiles/node_modules`,
+* so a broken or unwritable module fallback cannot stop that profile.
+*/
+function hostResolvedBaseUrl() {
+	return process.env.DSH_DESKTOP_HOST_RESOLVED === "1" ? pathToFileURL(INSTALL_ANCHOR).href : void 0;
+}
+/**
 * Initialize a missing profile from one shipped template. This copies only
 * the template's bundle list and patch-reload policy; local state from the
 * same-named shipped profile is not read, and no inheritance metadata is
@@ -231,7 +240,7 @@ function allPatches(composed) {
 */
 async function composeProfile(name, patchFiles, fromDefaultProfile) {
 	const profile = prepareProfile(name, true, fromDefaultProfile);
-	await healProfilesModuleFallback({
+	if (hostResolvedBaseUrl() === void 0) await healProfilesModuleFallback({
 		installAnchor: INSTALL_ANCHOR,
 		profile
 	});
@@ -316,7 +325,7 @@ async function runProfile(options) {
 			exit: (code) => void shutdown.shutdown(code),
 			ready: appReady.service
 		});
-	});
+	}, hostResolvedBaseUrl());
 	app.current = ctx;
 	if (composed.profile.patchReload === "live" && !signalShutdown.signal.aborted && ctx.fiber.state === 2 && ctx.get("loader") !== void 0) try {
 		if (ctx.get("hmr") === void 0) {
diff --git a/node_modules/@deepseek-ai/dsh/package.json b/node_modules/@deepseek-ai/dsh/package.json
index c85bb90..021a861 100644
--- a/node_modules/@deepseek-ai/dsh/package.json
+++ b/node_modules/@deepseek-ai/dsh/package.json
@@ -28,6 +28,14 @@
   },
   "license": "MIT",
   "dependencies": {
+    "dsh-image-generation": "0.1.0",
+    "dsh-ppt-composer": "0.1.1-rc.2",
+    "dsh-desktop-client-ui": "0.1.0",
+    "dsh-desktop-hmr-fallback": "0.1.0",
+    "dsh-desktop-log-bridge": "0.1.0",
+    "dsh-desktop-market-installer": "0.1.0",
+    "dsh-desktop-preset-transfer": "0.1.0",
+    "tintin-bundle": "0.1.0",
     "commander": "^15.0.0",
     "js-yaml": "^4.2.0",
     "node-addon-require-builtin": "^0.1.4",
