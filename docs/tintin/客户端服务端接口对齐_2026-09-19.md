# 客户端服务端接口对齐清单（2026-09-19 建立）

> 目的：盘点客户端实际调用的服务端端点与当前状态，登记待与服务端对齐的问题（问题驱动，逐条裁决后销项）。
> 复核方式：服务端在线后执行 `cd desktop && npm run contract:gen` 从实时 `/openapi.json` 重新生成
> `renderer/src/types/api-contract.generated.ts`，与本清单逐条核对（IRON-10 契约单一真实来源）。
> 时点说明：2026-09-19 本机服务端不可达（127.0.0.1:8766 未监听，服务端部署于远端），契约文件为旧版快照（479 paths）。

## 一、本周架构变更摘要（服务端需要知道的）

| 变更 | 日期/裁决 | 客户端现状 | 服务端需要做的 |
|---|---|---|---|
| **`/text_templates/match` 下线** | 2026-09-19 用户宣布接口将删 | 客户端全链已删（preload `textfxMatchKeywords`、主进程 handler、global.d.ts 类型、渲染层 fetchTextFxHits/缓存），**零调用** | 从服务端路由与 openapi 删除该端点；`text_template_match_id`（concat 复用回执）一并作废 |
| **关键词命中新架构** | 2026-09-19 用户裁决 | 词源=**产品资料关联关键词**（客户端选中产品带回，对字幕行窗口本地命中）；产品未关联词 → **LLM 兜底**（`/llm/chat/completions` 从口播文案提卖点词，数量随密度档位 3/8/12） | ①产品资料挂关键词并出字段；②见问题 Q1（服务端合成链路如何用产品关联词） |
| 本地词典兜底退役 | 2026-09-19 | `extractTextFxWords`（防水/快充/毫安类固定词典）整链删除 | 无 |
| 字幕轨 flag=1 | 2026-09-19 F3 改判 | 剪映草稿字幕轨带 `flag=1`（剪映「字幕轨」属性标记） | 无（草稿由客户端生成） |
| 型号不填商品编码 | 2026-09-19 报障修复 | 产品库选中回填型号时剥离尾部【编码】段；goods_no 不再兜底型号 | 无 |

## 二、端点清单（客户端实际调用）

调用层说明：**main**=主进程 httpRequest 直连；**renderer**=渲染层经 `window.tintin.server.get/post`（server:*/server-proxy 通用透传）；**API_PATHS**=`types/server-api.ts` 路径常量。

### 混剪/剪映链路（本周改动集中区）

| 端点 | 方法 | 层 | 用途 | 状态 |
|---|---|---|---|---|
| `/montage/split` | POST | main | 镜头智能分割 | 正常 |
| `/montage/concat` | POST | main | 服务端合成（字幕/花字/文字模板/BGM 整链） | 正常；**match_id 已不再下发**（见 Q1） |
| `/montage/bgm` `/montage/auto-mix` | POST | main | BGM/自动混 | 正常 |
| `/text_templates/templates` | GET/POST/DELETE | main | 文字模板库（列表/同步上传/删除） | 正常 |
| `/text_templates/render-preview` | POST | main | 命中行素材渲染 | ⚠️ 存量 500（Q3，2026-09-10 已登记） |
| ~~`/text_templates/match`~~ | POST | — | 关键词命中判定 | **已删除，客户端零调用** |
| `/templates/catalog` | GET | main | 模板 catalog 分组（花字库/文字模板分栏） | 正常 |
| `/fancy/templates` | GET | main | 花字模板库 | 正常 |
| `/subtitle_styles` | GET | main | 服务端字幕样式库（2026-09-17 裁决统一来源） | 正常 |
| `/config/luts` | GET | main | LUT 库清单 | 正常 |
| `/api/product-library/search` | GET | renderer | 产品库搜索（产品选择弹窗） | 正常；**响应 schema=unknown，见 Q2（keywords 字段名）** |
| `/api/product-library/*`（items/grouped/clients…） | GET | renderer | 产品资料页 | 正常 |
| `/api/storyboard/scripts` | GET | renderer | 脚本库 | 正常 |

### 语音/文案/LLM 链路

| 端点 | 方法 | 层 | 用途 | 状态 |
|---|---|---|---|---|
| `/copywriting/voiceover` | POST | main | 口播文案生成（product_desc+duration_s，服务端自持 prompt） | 正常 |
| `/indextts/tts` | POST | main | TTS（2026-09-05 起 /voxcpm/* 已废） | 正常 |
| `/voice/samples` | GET | main | 克隆参考声样本库 | 正常 |
| `/llm/chat/completions` | POST | main | 通用 LLM 通道（句子切分/AI 改写/**新增：关键词兜底提词**） | 正常 |
| `/script/adjust-copywriting` `/script/list` | POST/GET | main | 文案改写/脚本 | API_PATHS 有声明，**契约无对应业务域**（见 Q4） |
| `/whisper/transcribe` | POST | main | ASR | 正常 |
| `/audio/gen/bgm` | POST | main | AI 生成 BGM（MusicGen） | 正常 |
| `/audio/library` `/audio/categories` | GET | renderer | 音频库 | 正常 |

### 其余

`/material/*`（list/search/distinct/ocr/serve/stock_search/score-clip）、`/vsr/enhance|remove`、`/viral/clone/*`、`/workflow/run`、`/prompt/video`、`/health/capabilities|check`、`/stats/workbench`、`/config/fonts(+/upload)`、`/file`、`/api/logs/upload`、`/system/license` —— 正常；其中 `/health/*` `/stats/*` 等 14 条见 Q4。

## 三、待对齐问题清单（按优先级）

### Q1（P0·需裁决）match_id 下线后，服务端合成链路的关键词命中与客户端是两套判定

**现状**：客户端不再下发 `text_template_match_id`。服务端 concat 收到 `text_template_match_enabled + text_template_match_ids`（仅模板池）+ `subtitle_srt` 后，**退回旧口径自行命中**：常用关键词 ∪ 内置卖点词，不足由服务端 LLM 从字幕行补足（`text_template_match_llm` 默认开）。

**影响（三点）**：
1. **预览 ≠ 成片**：第四步效果预览轨与剪映草稿用的是客户端命中（产品关联词 + 客户端 LLM 兜底），服务端成片烧的是服务端自行命中——词、位置、数量都可能不同；
2. **产品关联词在服务端链路失效**：concat 契约没有词表字段（`text_template_words` 2026-09-11 已删），服务端不知道所选产品的关联词；
3. **确定性丧失**：原 match_id 机制（服务端存 events 7 天、concat 直接复用）保证预览=成片且同请求可复现；现在服务端 LLM 每次命中结果可能不同。

**候选方案（待裁决）**：
- **A（推荐）**：恢复下发词表——`text_template_words` / `fancy_words` 字段**仍在 concat 契约中**（2026-09-11 客户端停发，服务端字段未删；历史格式 `JSON.stringify(words)`），客户端把产品关联词（或 LLM 兜底提词结果）随请求下发。见下方「方案 A 接口支持度核查」；
- **B**：服务端在 concat 内按产品身份（brand/model 随请求下发或查会话）自行读产品资料关联词命中——客户端零改动，但要求服务端能取到「当前会话选中的产品」；
- **C**：维持现状，接受预览与成片不一致（不推荐）。

**方案 A 接口支持度核查（2026-09-19 更新：服务端已上线，live 契约 484 paths 实测）**：

| 层级 | 字段 | 服务端状态 | 客户端状态 |
|---|---|---|---|
| 事件直传 | `text_template_match_events: string` | ✅ **服务端同日新增**（描述原文：「客户端本地命中事件直传（方案 A）：JSON 数组 `[{"word","start","end","template_id"?}]`；传入后按事件烧制、跳过服务端命中（词源=客户端产品关键词）」） | ✅ 已下发（秒单位、template_id 随事件；无效窗口过滤） |
| 词表 | `text_template_words: string` | ✅ 在（事件优先，旧服务端回落口径） | ✅ 已下发（有效事件去重词表） |
| 花字词表 | `fancy_words: string` | ✅ 在 | ✅ 已下发（同一份命中词表） |
| `match_id` | `text_template_match_id` | ⚠ 历史兼容（TTL 7 天，「新集成请改传 events」） | ✅ 客户端零下发 |

**✅ Q1 客户端侧销项（2026-09-19 完成）**：`buildServerFxFields` 升级为事件直传——fxLines 过滤无效窗口后作 events 下发，词表从有效事件派生（两字段口径恒一致）；`text_template_match_llm` 等服务端命中参数不再起作用（事件模式跳过服务端命中）。**剩余**：实机验证预览轨与成片逐事件一致（跑一次服务端合成对照）。

**✅ Q2 销项（2026-09-19 实测）**：`GET /api/product-library/search` 响应 schema 已声明为 `ProductSearchOut`（items → `ProductItemOut`），其中 **`keywords: string[]`（default []）**——与客户端 `parseProductKeywords` 的第一字段名、数组格式完全一致，无需改码。

### Q2（P0·待信息）产品资料关联关键词的字段名

契约中 `GET /api/product-library/search` 响应 schema=`unknown`（服务端从未声明 response model），字段名无从对源。客户端已做防御解析：`keywords` / `product_keywords` / `关键词`，接受 `string[]` 或顿号/逗号/分号/换行分隔字符串（`opsProductLibraryLogic.parseProductKeywords`，单测钉住）。**请服务端确认实际字段名与格式**；若是其它名字，客户端一行对齐。建议服务端为该端点补 response model，契约即可自证。

### Q3（P1·存量）`/text_templates/render-preview` 500

2026-09-10 已登记（服务端内部错误，动画预览接口不可用）；当前渲染层已不依赖（词条=纯关键词标记，2026-09-15 裁决），仅主进程命中行素材下载消费。待服务端修复或从契约删除。

### Q4（P1·随 contract:gen 收敛）14 条 API_PATHS 声明无契约对应业务域

`/health/capabilities`、`/health/check`、`/stats/workbench`、`/script/adjust-copywriting`、`/script/list` 等 14 条：客户端声明了路径常量但旧版 openapi 无对应业务域——要么契约太旧（服务端已新增），要么常量超前（端点不存在）。contract:gen 后逐条销项。另有 444 条契约路径客户端未用（预期，不处理）。

### Q5（P2）`/montage/concat` 契约清理

服务端删除 match 后，建议同步从 openapi 删除 `text_template_match_id` 字段及其描述（"textfx_clips 按序轮换标注 template_id / events 保存 7 天 / match_id 复用"等标注全部作废），避免契约自相矛盾。

## 四、复核流程

1. 服务端在线 → `npm run contract:gen`；
2. 核对本清单 Q1–Q5 + API_PATHS 14 条 WARN；
3. 裁决结果回写本清单（销项留痕），涉及铁律 9 的先改文档再改代码。
