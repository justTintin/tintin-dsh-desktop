# 智能混剪拆分迁移映射（铁律 10 执行前置件，2026-09-18）

> 本文是 `useVideoMontage.ts`（3528 行）与 `VideoMontage.vue`（2257 行）拆分的**执行蓝图**。
> 依铁律 10：先有映射再动工；执行时**只许纯搬迁，禁止顺手重写**；每步落地 `npm run typecheck`
> 必须 0 错误 + `node --test tests/*.test.mjs` 全过。前序完成件见文末「进度」。

## 一、Phase 1（已完成 ✅ 92c6ee5）：videoMontageLogic.ts

1583 行 → 桶文件 19 行 + 5 子模块（common 63 / step1 349 / step2 339 / step3 190 / step4 692），
97 个导出逐一守恒，1027 测试全过。

## 二、useVideoMontage.ts 现状结构（2026-09-18 @ 1f8dbaf）

| 行段 | 内容 | 符号数 |
|---|---|---|
| L1-137 | 头注释 + import + **模块级**工具（`POLL_*`、`notify`、`unwrapIpc`、`errText`、`joinPath`、`PollChannel`） | 8（已在闭包外） |
| L139 | `export function useVideoMontage() {`（巨型闭包开始） | — |
| L140-230 | 共享：`serverUrl`/`ensureServerUrl`/`toAbsolute` + 轮询状态机（`polling`/`activeTaskId`/`statusText`/`pollTimer`/`pollCancelled`/`clearBusy`/`stopPolling`/`cancelPolling`/`startPolling`） | 15 |
| L232-630 | Step1 素材解析（含 `concatProgress`【实为 Step2 用】、`clearAllBusy`【跨步，见 §四】、预览弹窗簇、`downloadClipsToSplits`、`clearSplitCache`） | ~35 |
| L631-1262 | Step2 镜头重组：预合成/序列预览/镜头管理/确认合成（`submitConcatTask`/`downloadFinalChecked`/`withDeadline`/`pollResultEndpoint`/`confirmPlanOne`/队列）/口播文案弹窗 | ~55 |
| L1264-2221 | Step4 特效包装：BGM 持久化与 AI 生成、试听播放器、逐行 BGM、候选收集、`startFinalMix`（~170 行巨函数）、剪映导出双路径（`doJianyingExport`/`exportMontageTracksDraft`/`exportJianyingPackageDraft`）、`ensureVoiceRows`/`ensureProcessedSrt`/`subtitleAssetDir`/`plainJson`/`timelineDraftName` | ~65 |
| L2222-3428 | Step3 口播配音：voiceRows 扫描/参考样本/TTS 参数/字幕字体花字状态/文字模板全簇（match 缓存+防抖+样式橱窗）/克隆与配音批处理/字体 FontFace 设施/编辑与导出 | ~75 |
| L3424-3506 | `onUnmounted` + `return {…}`（约 230 个键，77 行） | — |
| L3509-3528 | 模块级类型 `FancyTemplateItem`、`SelectOptionLite` | 2 |

## 三、目标架构

```
composables/montage/
  context.ts               // MontageStepContext：serverUrl/ensureServerUrl/toAbsolute/
                           //   statusText/startPolling/stopPolling/cancelPolling/polling/
                           //   activeTaskId/notify/unwrapIpc/errText/joinPath + 跨步 ref 挂点
  useMontageStep1Split.ts  // 持有 Step1 refs/fns（含预览弹窗簇、edge-trim、splits 下载）
  useMontageStep2Concat.ts // 持有 Step2 refs/fns（预合成/确认/口播文案弹窗/序列预览）
  useMontageStep3Voice.ts  // 持有 Step3 refs/fns（扫描/样本/TTS/克隆/编辑/字体/花字/文字模板）
  useMontageStep4Final.ts  // 持有 Step4 refs/fns（BGM 全簇/混音/剪映导出双路径/SRT 资产）
useVideoMontage.ts         // 仅剩：context 组装 + 各步组合 + 跨步 watch + return 合并（目标 ≤900 行）
```

关键判定：**四步之间无循环依赖，跨步引用全部单向**（Step1←Step2←Step3←Step4 取数方向），
按 Step1→2→3→4 顺序逐个抽取，每抽一步 typecheck 一次、独立提交一次。

## 四、跨步耦合矩阵（拆分时必须走 context 或参数注入的符号）

| 符号 | 定义步 | 被跨步消费 | 处置 |
|---|---|---|---|
| `statusText` | 共享 | 全部四步 | context |
| `serverUrl`/`ensureServerUrl`/`toAbsolute` | 共享 | 全部四步 | context |
| `startPolling`/`stopPolling`/`cancelPolling`/`polling`/`activeTaskId` | 共享 | Step2（`submitConcatTask`） | context |
| `clearBusy`（可变闭包槽） | 共享 | Step2 三处赋值、`cancelPolling` 消费 | context（保持「槽」语义，勿改设计） |
| `clearAllBusy` | Step1 段内定义 | 操作 `concatBusy/confirmBusy/copyBusy`（S2）+ `finalBusy`（S4） | **移入 context 组装层**（晚于各步创建，注入回调） |
| `concatProgress` | Step1 段内定义 | 仅 Step2 流程使用 | 随 Step2 走 |
| `scenes`/`splitFps`/`splitResolution`/`srcVideos`/`splitsJobId` | Step1 | Step2（`runConcat`/`ensureSourceFps`/`submitConcatTask`/`confirmPlanOne`） | context 挂点（Step1 返回值注入 Step2） |
| `assemblePlans` | Step2 | Step3（确认产物 watch→`scanVoiceDir`、`enterStepVoice`）、Step4（`collectCandidates`/`ensureVoiceRows`/`refreshTextFxTracks`）、`timelineDraftName` 无关 | context 挂点 |
| `voiceRows`/`voiceDirInput` | Step3 | Step4 全簇（候选/BGM 目录/SRT 资产/`runDubBatch`/`nextVoiceChannel` 内写 Step4 进度） | context 挂点 |
| `runDubBatch`/`ensureVoiceRows`/`ensureProcessedSrt` | S3/S4 互调 | `startFinalMix`(S4)→`runDubBatch`(S3)；`runCloneBatch`(S3)→`ensureProcessedSrt`(S4) | context 互相注入（唯一双向点，保持现行为勿重排） |
| `nextVoiceChannel` | Step3 段内定义 | 回调内写 `finalBusy`/`finalVideoList`/`finalVideoPath`/`finalDone`/`statusText`（Step4+共享） | 随 Step3 走，注入 Step4 挂点 |
| `previewUrl` | Step1 | Step3（`playRowVideo`/`playDubbedVideo`） | context 挂点 |

## 五、逐符号迁移映射（按目标文件分组，执行时逐簇勾销）

### context.ts（约 120 行）
模块级：`POLL_INTERVAL_MS`、`POLL_TIMEOUT_MS`、`PollChannel`、`notify`、`unwrapIpc`、`errText`、`joinPath`；
闭包共享：`serverUrl`、`ensureServerUrl`、`toAbsolute`、`polling`、`activeTaskId`、`statusText`、
`pollTimer`、`pollCancelled`、`clearBusy`、`stopPolling`、`cancelPolling`、`startPolling`；
跨步挂点（声明类型，组装层赋值）：`scenes`、`splitFps`、`splitResolution`、`srcVideos`、
`splitsJobId`、`assemblePlans`、`voiceRows`、`voiceDirInput`、`previewUrl`、`clearAllBusy`。

### useMontageStep1Split.ts（约 480 行）
refs：`srcVideos`、`srcDurations`(+watch)、`threshold`、`minSceneLen`、`imageDuration`、`scenes`、
`scoreFilter`、`splitBusy`、`splitError`、`splitMsg`、`splitResolution`、`splitFps`、`splitsJobId`、
`splitsDownloading`、`previewUrl`、`previewTranscoding`、`previewToken`、`edgeTrimRunning`、
`edgeTrimFailCount`；
fns：`addVideos`、`selectFolder`、`onDrop`、`removeVideo`、`runSplit`、`maybeTrimEdgeClips`、
`updateSceneDesc`、`ensurePreviewSrc`、`previewSourceVideo`、`downloadClipsToSplits`、
`openSplitsDir`、`previewScene`、`closePreview`、`clearSplitCache`；
computed：`filteredScenes`。
（`checkedCount` 留主文件——它是 Step2 依赖的跨步 computed；`concatProgress`/`clearAllBusy` 移出。）

### useMontageStep2Concat.ts（约 640 行）
refs：`assembleLogic`、`concatLayout`、`concatFps`、`durationLimit`、`DURATION_LIMITS`、`batchCount`、
`randomness`、`concatTransition`、`concatBusy`、`confirmBusy`、`copyBusy`、`concatError`、
`edgeSpeedup`、`EDGE_SPEEDUP_OPTIONS`、`TRANSITIONS`、`assemblePlans`(+watch)、`currentPlanIdx`、
`detailDragFrom`、`seqClips`、`seqIdx`、`seqSrc`、`planConfirmQueue`、`concatProgress`、
`sharedProductInfo`、`productDlg`、`copyViewDlg`、`planMenu`；
computed：`checkedCount`（注入 scenes）、`recBatchCount`(+watch)、`currentPlan`、`hasUnconfirmed`、
`confirmedPaths`、`concatResults`；
fns：`runConcat`、`planDurText`、`planRowText`、`selectPlan`、`setSeqClip`、`startSeqPreview`、
`onSeqEnded`、`markPlanDirty`、`onDetailDragStart/End`、`onDetailDrop`、`toggleClipDeleted`、
`planClipUrls`、`ensureSourceFps`、`submitConcatTask`、`downloadFinalChecked`、`withDeadline`、
`pollResultEndpoint`、`confirmPlanOne`、`confirmAllPrecompose`、`confirmPlanSingle`、
`openProductDlg`、`closeProductDlg`、`genCopyForPlan`、`productDlgGenerate`、`viewPlanCopy`、
`closeCopyView`、`openPlanMenu`、`closePlanMenu`。

### useMontageStep3Voice.ts（约 1100 行，若超 1000 再按「文字模板簇」拆出 `useMontageTextFx.ts`，约 380 行）
refs：`voiceDirInput`、`selectedVoiceFiles`、`voicesDir`、`voiceRows`、`refSamples`、
`selectedRefSample`、`refAudioPath`、`refAudioLabel`、`refPreviewUrl`、`refText`、`ttsApiUrl`、
`ttsSteps`、`ttsCfg`、`ttsSpeedMin`、`ttsSpeedMax`、`addSubtitles`、`subtitleFont`、`fontOptions`、
`fontsLoading`、`fancyEnabled`、`fancyStyle`、`subtitleStyleKey`、`subtitleStylePresets`、
`subtitleAnimKey`、`subtitleFontSize`、`fancyPosition`、`subtitleBgOpacity`、`fancyTemplateId`、
`fancyTemplates`、`fancyPreviews`、`fancyTemplatesLoading`、`textFxEnabled`、`lutRestore`、`lutId`、
`lutList`、`lutListLoading`、`textTemplateId`、`textRandomCount`、`textKeywordDensity`、
`textTemplates`、`textTemplatesLoading`、`catalogTextLanes`、`textFxPreviewTracks`、
`textFxTrackSeq`、`rewriteTemp`、`aiRewriteDlg`、`ttsEngine`、`ttsDurationFactor`、`ttsEmoText`、
`ttsEmoAlpha`、`ttsPauseMs`、`cloneParamsDlg`、`editDlg`、`voiceBusy`、`rewriteBusy`、
`offVoiceProgress`、`voiceProgress`、`voiceTotal`、`voiceDone`、`fontsPreloaded`、
`subtitleStylesPreloaded`、`ns*`（6 个样本上传 ref）、`fontFace` 设施（3 Set + version）；
computed：`activeTextPool`、`activeTextCount`、`textTemplateOptions`、`textFxStyleSamples`、
`selectedSubtitlePreset`、`subtitlePreviewStyle`、`selectedFancyTemplate`；
fns/watch：`loadLuts`(+watch)、`catalogLaneIdOf`、`loadCatalogLanes`、`extractTextFxWords`、
`textFxHitsForExport`、`fetchTextFxHits`、`textFxHitsKey`、`currentMatchTemplateIds`、
`refreshTextFxTracks`(+watch 800ms 防抖)、`ensureSrvBase`、`srvAbs`、`loadTextTemplates`、
`nextVoiceChannel`、`ensureTtsApiUrl`、`confirmedVoiceDirs`、`scanVoiceDir`(+assemblePlans watch)、
`enterStepVoice`、`loadRefSamples`、`extractArrayLike`、`selectRefAudio`、样本上传 4 fns、
改写/克隆参数弹窗 6 fns、`batchAiRewrite`、`runCloneBatch`、`startSynthesizeVoice`、
`selectedFontFamily`、`ensureServerFontFace`、`fontOptionCssFamily`、`fontOptionStyle`、
`refreshFonts`、`refreshSubtitleStyles`、`loadFancyTemplates`、`openEditDlg`、`saveEditDlg`、
`exportVoice`、`playRowVideo`、`playDubbedVideo`、`toggleLengthMode`、`lengthModeTip`、`regenVoice`；
模块级类型：`FancyTemplateItem`、`SelectOptionLite`（→ `montage/types.ts` 或随 step3）。
非响应式缓存随簇走：`textFxHitsByVideo`、`textFxHitsCache`、`lastMatchTemplateIds`、
`textFxHitsKey`、`CATALOG_LANE_TPL`、`serverFontFamilies`。

### useMontageStep4Final.ts（约 950 行）
refs：`bgmPath`(+watch localStorage)、`bgmName`、`bgmVolume`(+watch)、`rowBgm`、`finalBusy`、
`finalMode`、`finalDone`、`finalProgress`、`exportBusy`、`exportProgress`、`exportStage`、
`lastExportDraftPath`、`exportDoneMsg`、`finalVideoList`、`finalVideoPath`、`finalSelIdx`、
`finalPreviewUrl`、`finalPreviewTitle`、`bgmSource`、`bgmGenPrompt`、`bgmGenStyle`、
`bgmGenDuration`、`bgmGenBusy`、`bgmGenError`、`bgmGenUrl`、`bgmGenMeta`、`bgmAudioEl`、
`bgmPlaying`、`bgmPosMs`、`bgmDurMs`、`step4Candidates`、`lastComposeTasks`；
computed：`bgmPreviewUrl`；
fns：`generateBgm`、`downloadLibraryBgm`、`applyLibraryBgm`、`pickBgm`、`rowBgmName`、`setRowBgm`、
`clearRowBgm`、`pickRowBgm`、`rowBgmOverride`、`rowBgmForCandidate`、`toggleBgmPlay`、
`stopBgmPlay`、`onBgmVolumeInput`、`seekBgm`、`collectCandidates`、`enterStep4`、`startFinalMix`、
`onMixFinished`、`onMixError`、`openFinalDir`、`openExportDraftDir`、`exportAllToJianyingDraft`、
`doJianyingExport`、`jianyingFxParams`、`plainJson`、`timelineDraftName`、`ensureVoiceRows`、
`subtitleAssetDir`、`ensureProcessedSrt`、`exportMontageTracksDraft`、
`exportJianyingPackageDraft`、`previewFinalVideo`。

### useVideoMontage.ts（目标 ≤900 行）
context 组装 → 四步组合（Step1→2→3→4 顺序，后步注入前步产物）→ `clearAllBusy`（组装层，
引四步 busy）→ `onUnmounted` → `return { …step1, …step2, …step3, …step4, 共享 }`
（**return 键集合一字不变**，.vue 解构零改动——这是 Phase3 的安全前提）。

## 六、执行顺序与验收门

| # | 动作 | 验收 |
|---|---|---|
| E1 | 建 `montage/context.ts` + Step1 抽取 | typecheck 0 + 测试全过 + return 键集合 diff 为空 |
| E2 | Step2 抽取 | 同上 |
| E3 | Step3 抽取（超 1000 则先拆 textfx 子模块） | 同上 |
| E4 | Step4 抽取 + 主文件瘦身 | 同上 + `useVideoMontage.ts ≤1000 行` |
| E5 | 运行时冒烟：四步向导手工回归 | 解析→重组→口播→合成全链 |

每个 Ei 独立提交可独立回滚。**禁止**：改任何函数体、改任何 ref 语义、改 return 键名、
顺手重命名。Phase 3（VideoMontage.vue 模板 1045 行抽 4 个步骤面板组件）另立映射，待 Phase2 完成后做。

## 七、进度

- [x] Phase 1：videoMontageLogic.ts 拆分（92c6ee5，2026-09-18）
- [x] Phase 2 前置：本映射文档
- [ ] Phase 2 E1-E4：useVideoMontage.ts 拆分（建议满上下文新会话执行，避免方案B式的细节丢失）
- [ ] Phase 3：VideoMontage.vue 拆分
