# 剪映轨道格式标准（2026-09-17 修订）

> **强制标准**（用户裁决 2026-09-16）：「剪映的轨道的所有格式的定义要形成标准，导出时根据此标准导出，而不是随便复制一个作为模板。」
> 本文是剪映草稿**轨道 / 片段 / 素材**字段的**唯一权威定义**；导出器实现必须与本文逐条对应。
> 依据来源：pyJianYingDraft 0.3.0 **全量源码**（segment.py / track.py / local_materials.py / text_segment.py / video_segment.py / audio_segment.py / effect_segment.py / animation.py / keyframe.py / script_material.py / script_file.py / template_mode.py / time_util.py / draft_folder.py / metadata/effect_meta.py）+ 本机剪映 11.5.5 实测解密草稿（9月17日-电商模板）+ key_value.json 素材分类体系 + 用户裁决。
> 变更流程（《工程规范铁律》铁律 9）：改标准 → 改导出器 → 单测钉住 → 登记《剪映互通整体方案》附录 H。

---

## 0. 总则

1. **一律按标准构造**：任何 track / segment / material 对象必须由导出器的**标准构造器**生成（见 §8 映射表）；**禁止手写段对象、禁止克隆其他草稿当模板**（2026-09-16 封面事故裁决）。
2. **最终裁判**：字段形状以本文为准；争议时以「本机剪映 11.x 实测能打开 + 轨道完整」为最终裁判。
3. **服务端只出素材+语义清单，不出 draft 结构**（2026-09-17 用户裁决 B）：from-tasks 路径中，服务端接口（`/editor/export/jianying/from-task`）的职责收敛为**素材文件（含 sha256）+ 段语义清单 + 事件表**（segments：asset 引用/入点出点/音量/归属窗口；events：文字模板与花字的命中结果+时机——命中判定在服务端，清单必须承载，否则客户端重算即成另一个双实现）；**draft_content 的 tracks/segments/materials 全部由客户端标准构造器生成**（条 1 的绝对化）。契约覆盖面（九类产出落点对照）与清单自检要求见互通方案 §3.6 P1。
   - **过渡期**（服务端契约改造完成前）：现有直传段仍按旧规则处理——本地只做「窗口偏移 + 时长裁剪 + 路径绝对化」，不重写字段；违反本标准的特征由 `auditDraftStandardConformance` 上报不阻断。
   - 裁决理由：标准唯一权威实现在客户端（构造器 + 单测 + golden 门禁），双实现必然漂移；落盘路径/多任务合并/文字模板三件套/目标剪映版本均为客户端职责。
4. **id 规范**：草稿内所有 id = **32 位小写 hex**（对照 uuid4().hex）；草稿目录名 = draft_name（命名规范见互通方案 §0.1）。
   - **例外**：剪映 11.x 实测草稿中部分 id 为**大写 UUID 带连字符**格式（如 `E1C41F84-6A64-4d0b-ADF1-A44B35252A0A`），这是剪映自身生成的格式；导出器仍统一用小写 hex。
5. **时间单位**：微秒（us）整数。`target_timerange` = 段在时间轴的位置；`source_timerange` = 段在素材文件内部的起点/时长。
6. **静默不造假**：素材/预设缺失时跳过该段（不生成占位数据）；跳过必须可排障（日志或返回值）。

---

## 1. 草稿顶层结构

### 1.1 draft_content.json 骨架

骨架 = `desktop/main/jianying-draft-template.js`（来源 pyJianYingDraft 0.3.0 `assets/draft_content_template.json`）。

**强制覆写**（导出时）：`id`（新 hex）、`name`（草稿名）、`duration`（总时长 us，§6）、`create_time`/`update_time`（秒）、`fps=30`、`canvas_config`（首视频宽高 + `ratio`：9:16 / 16:9 / 1:1）、`tracks`、`materials`。

**版本三元组**（防剪映判「版本过低/已损坏」拒开）：`new_version='110.0.0'`、`version=360000`、`platform={app_id:3704, app_source:'lv', app_version:'5.9.0', os:'windows'}`、`last_modified_platform` 同 platform。

常量出处：`jianying-exporter.js` 顶部 `DRAFT_SCHEMA`。

### 1.2 顶层字段全集（pyJianYingDraft 模板 28 个 + 11.x 新增 8 个 = 36 个）

| 字段 | 类型 | 5.9 模板 | 11.x 实测 | 说明 |
|---|---|---|---|---|
| canvas_config | object | `{height, width, ratio}` | 同左 + `background:null` | 画布配置 |
| color_space | int | `0` | `0` | 色彩空间 |
| config | object | 24 子字段 | 27 子字段 | 见 §1.3 |
| cover | null | `null` | — | 封面 |
| create_time | int/float | `0` | `1789610553260373` | 创建时间（11.x 为微秒级时间戳） |
| draft_type | string | — | `"video"` | **11.x 新增** |
| duration | int | `0` | `33700000` | 总时长 us |
| extra_info | null | `null` | — | 额外信息 |
| fps | float | `30.0` | — | 帧率 |
| free_render_index_mode_on | bool | `false` | — | 自由渲染索引模式 |
| function_assistant_info | object | — | 38 子字段 | **11.x 新增**，AI 功能开关集合 |
| group_container | null | `null` | — | 组容器 |
| id | string | UUID | UUID | 草稿 id |
| is_drop_frame_timecode | bool | — | `false` | **11.x 新增** |
| keyframe_graph_list | array | `[]` | — | 关键帧图列表 |
| keyframes | object | 8 子类 | — | 关键帧容器 |
| last_modified_platform | object | platform 四元组 | 八元组 | 最后修改平台 |
| lyrics_effects | array | — | `[]` | **11.x 新增** |
| materials | object | 45 种 | 57 种 | 素材字典，见 §4 |
| mixed_track_mode_on | bool | — | `false` | **11.x 新增** |
| mutable_config | null | `null` | — | 可变配置 |
| name | string | `""` | — | 草稿名 |
| new_version | string | `"110.0.0"` | `"188.0.0"` | 新版本标识 |
| path | string | — | `""` | **11.x 新增** |
| platform | object | 四元组 | 八元组 | 平台信息 |
| relationships | array | `[]` | — | 关系 |
| render_index_track_mode_on | bool | `false` | `true` | 渲染索引轨道模式 |
| retouch_cover | null | `null` | — | 修饰封面 |
| smart_ads_info | object | — | `{draft_url, page_from, routine}` | **11.x 新增** |
| source | string | `"default"` | — | 来源 |
| static_cover_image_path | string | `""` | — | 静态封面路径 |
| time_marks | null | `null` | — | 时间标记 |
| tracks | array | `[]` | 6 条 | 轨道列表，见 §2 |
| uneven_animation_template_info | object | — | 4 子字段 | **11.x 新增** |
| update_time | int | `0` | — | 更新时间 |
| version | int | `360000` | `360000` | 版本号 |

> **注意**：11.x 实测草稿中，空值字段（null/空数组/空字符串）会被剪映**省略不写**。导出器仍按完整骨架输出，剪映打开后自行裁剪。
> **来源注记（2026-09-17 对源核验）**：上表「11.x 实测」列中 `draft_type`、`is_drop_frame_timecode`、`lyrics_effects`、`mixed_track_mode_on`、`smart_ads_info`、`function_assistant_info`（§1.5 的 38 子字段）与 `uneven_animation_template_info` 的结构取值，来自草稿目录内 `template.tmp`（3984 字节明文空白骨架，抓取当日即被剪映清理，**已不可再现**）。当前解密版 `draft_content.json` 中这些字段的状态为：前四者省略，`smart_ads_info = {}`，`function_assistant_info = {"fps":{}}`，`uneven_animation_template_info = {}`（默认值省略后仅剩空壳）；`create_time` 亦省略，微秒级时间戳实际位于 `draft_meta_info.json` 的 `tm_draft_create`/`tm_draft_modified`（实测 `1789616827751390`）。

### 1.3 config 子字段（5.9 模板 24 个 + 11.x 新增 3 个 = 27 个）

| 字段 | 类型 | 标准值 | 说明 |
|---|---|---|---|
| adjust_max_index | int | `1` | 调节最大索引 |
| attachment_info | array | `[]` | 附件信息 |
| combination_max_index | int | `1` | 组合最大索引 |
| export_range | null | `null` | 导出范围 |
| extract_audio_last_index | int | `1` | 提取音频最后索引 |
| hdr_vivid | bool | `false` | **11.x 新增**，HDR 生动 |
| lyrics_recognition_id | string | `""` | 歌词识别 id |
| lyrics_sync | bool | `true` | 歌词同步 |
| lyrics_taskinfo | array | `[]` | 歌词任务信息 |
| maintrack_adsorb | bool | `true` | 主轨磁吸 |
| material_save_mode | int | `0` | 素材保存模式 |
| multi_language_current | string | `"none"` | 当前多语言 |
| multi_language_list | array | `[]` | 多语言列表 |
| multi_language_main | string | `"none"` | 主多语言 |
| multi_language_mode | string | `"none"` | 多语言模式 |
| original_sound_last_index | int | `1` | 原声最后索引 |
| record_audio_last_index | int | `1` | 录音最后索引 |
| sticker_max_index | int | `1` | 贴纸最大索引 |
| subtitle_keywords_config | null | `null` | 字幕关键词配置 |
| subtitle_recognition_id | string | `""` | 字幕识别 id |
| subtitle_sync | bool | `true` | 字幕同步 |
| subtitle_taskinfo | array | `[]` | 字幕任务信息 |
| system_font_list | array | `[]` | 系统字体列表 |
| use_float_render | bool | `false` | **11.x 新增**，浮点渲染 |
| video_mute | bool | `false` | 视频静音 |
| voice_change_sync | bool | `false` | **11.x 新增**，变声同步 |
| zoom_info_params | null | `null` | 缩放信息参数 |

### 1.4 platform 子字段

| 字段 | 5.9 模板 | 11.x 实测 | 说明 |
|---|---|---|---|
| app_id | `3704` | `3704` | 应用 id |
| app_source | `"lv"` | `"lv"` | 应用来源 |
| app_version | `"5.9.0"` | `"11.4.2"` / `"11.5.5"` | 应用版本 |
| os | `"windows"` | `"windows"` | 操作系统 |
| os_version | — | `"10.0.26200"` | **11.x 新增** |
| device_id | — | 32 位 hex | **11.x 新增** |
| hard_disk_id | — | `""` | **11.x 新增** |
| mac_address | — | 逗号分隔两个 hex | **11.x 新增** |

### 1.5 function_assistant_info 子字段（11.x 新增，38 个）

AI 功能开关集合，导出器默认全部关闭/空值：

`audio_noise_segid_list:[]`, `auto_adjust:false`, `auto_adjust_fixed:false`, `auto_adjust_fixed_value:50.0`, `auto_adjust_segid_list:[]`, `auto_caption:false`, `auto_caption_segid_list:[]`, `auto_caption_template_id:""`, `caption_opt:false`, `caption_opt_segid_list:[]`, `color_correction:false`, `color_correction_fixed:false`, `color_correction_fixed_value:50.0`, `color_correction_segid_list:[]`, `deflicker_segid_list:[]`, `enhance_quality:false`, `enhance_quality_fixed:false`, `enhance_quality_segid_list:[]`, `enhance_voice_segid_list:[]`, `enhande_voice:false`, `enhande_voice_fixed:false`, `eye_correction:false`, `eye_correction_segid_list:[]`, `fixed_rec_applied:false`, `fps:{den:1, num:0}`, `normalize_loudness:false`, `normalize_loudness_audio_denoise_segid_list:[]`, `normalize_loudness_fixed:false`, `normalize_loudness_segid_list:[]`, `retouch:false`, `retouch_fixed:false`, `retouch_segid_list:[]`, `smart_rec_applied:false`, `smart_segid_list:[]`, `smooth_slow_motion:false`, `smooth_slow_motion_fixed:false`, `video_noise_segid_list:[]`

> 来源：`template.tmp` 明文空白骨架（已失效，见 §1.2 来源注记）；当前实测草稿中该字段仅剩 `{"fps":{}}`。导出器按本清单全量输出默认值。

### 1.6 draft_meta_info.json 骨架

来源 pyJianYingDraft `assets/draft_meta_info.json`（66 行）。核心字段：

| 字段 | 类型 | 说明 |
|---|---|---|
| draft_id | string | 草稿 UUID（大写带连字符） |
| draft_name | string | 草稿名称 |
| draft_cover | string | 封面文件名 |
| draft_root_path | string | 草稿根路径 |
| draft_fold_path | string | 草稿折叠路径 |
| draft_materials | array | 素材库（7 种 type：0/1/2/3/6/7/8） |
| draft_cloud_* | 系列 | 云端相关（导出器留空） |
| draft_enterprise_info | object | 企业信息 |
| draft_is_* | bool 系列 | 各种标记 |
| tm_* | 系列 | 时间戳 |

> **导出器最小 meta 形状（2026-09-17 对齐收尾登记）**：两条导出路径实际写出的 `draft_meta_info.json` 为 10 字段最小形状：`id`、`draft_name`、`draft_fold_path`、`draft_root_path`、`draft_type`（`"face"`）、`create_time`、`update_time`、`tm_draft_modified`（毫秒）、`platform`（`"windows"`）。字段名与骨架一致（此前误写 `draft_foldpath`/`draft_rootpath`，已修正）；剪映首开保存后会自行改写补齐为完整结构（与附录 A 实测一致），故不强制输出 66 行全量骨架。from-tasks 路径以服务端 `draft_meta_info` 为基底仅覆写同名字段。

---

## 2. 轨道（track）

### 2.1 轨道类型枚举（TrackType）

来源 pyJianYingDraft `track.py` TrackType 枚举：

| 类型 | 片段类型 | 允许修改 | 说明 |
|---|---|---|---|
| `video` | VideoSegment | ✅ | 视频轨 |
| `audio` | AudioSegment | ✅ | 音频轨 |
| `text` | TextSegment | ✅ | 文本轨 |
| `sticker` | StickerSegment | ❌ | 贴纸轨 |
| `effect` | EffectSegment | ❌ | 特效轨（独立轨道，apply_target_type=2） |
| `filter` | FilterSegment | ❌ | 滤镜轨（独立轨道） |
| `adjust` | None | ❌ | 调节轨（仅供导入，不要新建） |

### 2.2 轨道字段（Track.export_json()）

| 字段 | 类型 | 标准值 | 说明 |
|---|---|---|---|
| id | string | `hexId()` | 32 位小写 hex |
| type | enum | 见 §2.1 | 轨道类型 |
| attribute | int | `0` | 0=默认 **1=静音** 2=隐藏 4=锁定 |
| flag | int | `0` | 保留（11.x 实测字幕轨 flag=1） |
| is_default_name | bool | `true` | 默认名标记 |
| name | string | `''` | 空 = 剪映显示「未命名轨道」 |
| segments | array | `[segment…]` | 见 §3 |

> **11.x 实测差异**：实际草稿中 track 仅包含 `id`, `type`, `is_default_name`, `segments` 四个必有字段；`attribute`, `flag`, `name` 在默认值时被省略。导出器仍按完整 7 字段输出。

### 2.3 导出器实际产出的轨道清单

| 轨 | type | 产出函数（jianying-exporter.js / montage-final-ipc.js） |
|---|---|---|
| 主视频轨 | video | `exportMultiToDraft` §5 / `mergeJianyingManifests` |
| 字幕轨 | text | `appendSubtitleTrack`（经 `buildSubtitleSegment`）；**flag=1**（2026-09-19 F3 改判，见 §2.2） |
| 关键词花字轨 | text | `appendKeywordTrack(kind='fancy')` |
| 文字模板轨 | text | `appendTextTemplateSegments` |
| 花字事件轨 | text | `appendFancyEventTracks` |
| 口播轨 | audio | `exportMultiToDraft` §5（voiceClips）/ merge |
| 音效轨 | audio | `appendFancyEventTracks` |
| BGM 轨 | audio | `appendBgmTrack` |
| 贴纸轨 | sticker | `buildStickerTrackFromPreset` |
| 特效 | （不独立成轨） | `applyVideoEffect`：video_effects 素材挂主轨全部段 `extra_material_refs` |
| 转场 | （不独立成轨） | `buildTransitionMaterial`：挂「前一段」`extra_material_refs` |
| 滤镜 | **未实现（登记待做）** | — |

### 2.4 11.x 实测轨道实例（电商模板）

| 轨道 | type | segments | 说明 |
|---|---|---|---|
| Track[0] | video | 14 | 主视频轨（14 段视频） |
| Track[1] | text | 15 | 字幕轨（15 段字幕，flag=1） |
| Track[2] | text | 8 | 文字模板轨（8 段文字模板） |
| Track[3] | audio | 11 | 口播轨（11 段 TTS 配音） |
| Track[4] | audio | 1 | BGM 轨（1 段背景音乐） |
| Track[5] | audio | 9 | 音效轨（9 段音效） |

---

## 3. 片段（segment）

### 3.1 通用基类字段（BaseSegment.export_json()）

来源 pyJianYingDraft `segment.py` BaseSegment：

| 字段 | 类型 | 标准值 | 说明 |
|---|---|---|---|
| id | string | `hexId()` | 片段全局 id |
| material_id | string | 素材 id | **必须存在**于 materials.*（§7-⑤ 校验） |
| target_timerange | object | `{start, duration}` us | 段在时间轴的位置 |
| enable_adjust | bool | `true` | 启用调节 |
| enable_color_correct_adjust | bool | `false` | 启用色彩校正调节 |
| enable_color_curves | bool | `true` | 启用色彩曲线 |
| enable_color_match_adjust | bool | `false` | 启用色彩匹配调节 |
| enable_color_wheels | bool | `true` | 启用色轮 |
| enable_lut | bool | `true` | 启用 LUT |
| enable_smart_color_adjust | bool | `false` | 启用智能色彩调节 |
| last_nonzero_volume | float | `1.0` | 最后非零音量 |
| reverse | bool | `false` | 反转 |
| track_attribute | int | `0` | 轨道属性：0=默认 **1=静音** 2=隐藏 4=锁定 |
| track_render_index | int | `0` | 轨道渲染索引 |
| visible | bool | `true` | 可见 |
| common_keyframes | array | `[]` | 关键帧列表 |
| keyframe_refs | array | `[]` | 关键帧引用 |

> **11.x 实测差异**：实际草稿中 segment 字段更精简，默认值字段被省略。11.x 实测段字段并集（18 个）：`clip, enable_adjust, enable_adjust_mask, enable_hsl, enable_lut, extra_material_refs, hdr_settings, id, material_id, render_index, render_timerange, responsive_layout, source, source_timerange, target_timerange, track_render_index, uniform_scale, volume`。
>
> **新增字段**（11.x 实出有而 pyJianYingDraft 未定义）：
> - `enable_adjust_mask`：启用调节蒙版
> - `enable_hsl`：启用 HSL
> - `render_timerange`：渲染时间范围
> - `responsive_layout`：响应式布局
> - `source`：来源标记

### 3.2 媒体段附加字段（MediaSegment.export_json()）

来源 pyJianYingDraft `segment.py` MediaSegment，适用于视频段和音频段：

| 字段 | 类型 | 标准值 | 说明 |
|---|---|---|---|
| source_timerange | object/null | `{start: 0, duration: dur}` | 段在素材内部的起点/时长 |
| speed | float | `1.0` | 播放速度 |
| volume | float | `1.0` | 音量（有口播的素材段 `0`，见 §6） |
| extra_material_refs | array | `[speedId, …]` | 附加素材引用（speed 素材 id 必须存在，§7-⑤） |
| is_tone_modify | bool | `false` | 是否变调 |

> **volume 取值口径（2026-09-17 实测补注）**：`1.0` = 原始音量，`0` = 静音，**可以大于 1**（放大）。实测口播段 `volume = 6.098666191101074`（float32 运算残差，说明剪映内部按 float32 计算存储）。导出器仍以 `1.0` 为默认，BGM 增益等场景按计算值输出（§6）。

### 3.3 视觉段附加字段（VisualSegment.export_json()）

来源 pyJianYingDraft `segment.py` VisualSegment，适用于视频段、贴纸段、文本段：

```json
{
  "clip": {
    "alpha": 1.0,
    "flip": { "horizontal": false, "vertical": false },
    "rotation": 0.0,
    "scale": { "x": 1.0, "y": 1.0 },
    "transform": { "x": 0.0, "y": 0.0 }
  },
  "uniform_scale": { "on": true, "value": 1.0 }
}
```

**ClipSettings 字段说明**（来源 `segment.py` ClipSettings）：

| 字段 | 类型 | 标准值 | 说明 |
|---|---|---|---|
| alpha | float | `1.0` | 不透明度 0-1 |
| flip.horizontal | bool | `false` | 水平翻转 |
| flip.vertical | bool | `false` | 垂直翻转 |
| rotation | float | `0.0` | 顺时针旋转角度 |
| scale.x | float | `1.0` | 水平缩放 |
| scale.y | float | `1.0` | 垂直缩放 |
| transform.x | float | `0.0` | 水平位移，单位=半画布宽 |
| transform.y | float | `0.0` | 垂直位移，单位=半画布高，y 负=向下 |

**特殊位置**：
- **字幕段**：`clip.transform = {x: 0, y: -0.8}`（`SUBTITLE_TRANSFORM_Y`；pyJianYingDraft `ClipSettings` 官方口径，单位=半画布，y 负=向下）。
- **贴纸段**：`clip` 由 R2 公式换算（设计画布 720×720、y 向上）。
- **视频段**：另加 `hdr_settings = {intensity: 1.0, mode: 1, nits: 1000}`。

### 3.4 视频段特有字段（VideoSegment.export_json()）

来源 pyJianYingDraft `video_segment.py` VideoSegment：

```json
{
  "hdr_settings": { "intensity": 1.0, "mode": 1, "nits": 1000 }
}
```

视频段可附加的素材（通过 `extra_material_refs` 引用）：
- `Speed`（必须）→ materials.speeds
- `VideoEffect` → materials.video_effects
- `Filter` → materials.effects
- `MixMode` → materials.effects
- `Mask` → materials.masks（5.9）/ materials.common_mask（11.x）
- `Transition` → materials.transitions（挂在**前一段**）
- `BackgroundFilling` → materials.canvases
- `Chroma` → materials.chromas
- `AudioFade` → materials.audio_fades
- `SegmentAnimations` → materials.material_animations

### 3.5 音频段特有字段（AudioSegment.export_json()）

来源 pyJianYingDraft `audio_segment.py` AudioSegment：

```json
{
  "clip": null,
  "hdr_settings": null
}
```

音频段可附加的素材（通过 `extra_material_refs` 引用）：
- `Speed`（必须）→ materials.speeds
- `AudioEffect` → materials.audio_effects
- `AudioFade` → materials.audio_fades

### 3.6 文本段特有字段（TextSegment.export_json()）

来源 pyJianYingDraft `text_segment.py` TextSegment：

文本段继承 VisualSegment，无额外字段。其素材（materials.texts）由 `export_material()` 生成，见 §4.3。

文本段可附加的素材（通过 `extra_material_refs` 引用）：
- `Speed`（必须）→ materials.speeds
- `TextBubble` → materials.effects（type='text_shape'）
- `TextEffect` → materials.effects（type='text_effect'）
- `SegmentAnimations` → materials.material_animations

### 3.7 贴纸段特有字段（StickerSegment.export_json()）

来源 pyJianYingDraft `video_segment.py` StickerSegment：

贴纸段继承 VisualSegment，`source_timerange=null`。其素材（materials.stickers）由 `export_material()` 生成，见 §4.4。

### 3.8 特效段（EffectSegment）与滤镜段（FilterSegment）

来源 pyJianYingDraft `effect_segment.py`：

**EffectSegment**（独立特效轨道上的段）：

| 字段 | 类型 | 标准值 | 说明 |
|---|---|---|---|
| （继承 BaseSegment 全部字段） | | | |
| material_id | string | VideoEffect.global_id | 指向 materials.video_effects 中的特效素材 |

- **apply_target_type=2**（全局作用域），与 VideoSegment 上的特效（apply_target_type=0）区分
- 特效素材定义见 §4.5 video_effects

**FilterSegment**（独立滤镜轨道上的段）：

| 字段 | 类型 | 标准值 | 说明 |
|---|---|---|---|
| （继承 BaseSegment 全部字段） | | | |
| material_id | string | Filter.global_id | 指向 materials.effects 中的滤镜素材 |

- 滤镜素材定义见 §4.6 effects（type='filter'）

### 3.9 关键帧系统（Keyframe / KeyframeList）

来源 pyJianYingDraft `keyframe.py`：

**Keyframe**（单个关键帧）：

| 字段 | 类型 | 标准值 | 说明 |
|---|---|---|---|
| id | string | `hexId()` | 关键帧全局 id |
| time_offset | int | 微秒 | 相对于素材起始点的时间偏移 |
| values | float[] | `[value]` | 关键帧的值（通常单元素） |
| curveType | string | `"Line"` | 插值类型（目前仅支持线性） |
| graphID | string | `""` | 图 id（保留） |
| left_control | object | `{x: 0.0, y: 0.0}` | 左控制点 |
| right_control | object | `{x: 0.0, y: 0.0}` | 右控制点 |

**KeyframeProperty**（关键帧属性枚举）：

| 属性 | 值 | 适用段类型 | 说明 |
|---|---|---|---|
| position_x | `KFTypePositionX` | VisualSegment | 水平位置，单位=半画布宽 |
| position_y | `KFTypePositionY` | VisualSegment | 垂直位置，单位=半画布高 |
| rotation | `KFTypeRotation` | VisualSegment | 顺时针旋转角度 |
| scale_x | `KFTypeScaleX` | VisualSegment | 水平缩放（与 uniform_scale 互斥） |
| scale_y | `KFTypeScaleY` | VisualSegment | 垂直缩放（与 uniform_scale 互斥） |
| uniform_scale | `UNIFORM_SCALE` | VisualSegment | 统一缩放（与 scale_x/y 互斥） |
| alpha | `KFTypeAlpha` | VideoSegment | 不透明度 0-1 |
| saturation | `KFTypeSaturation` | VideoSegment | 饱和度 -1 到 1 |
| contrast | `KFTypeContrast` | VideoSegment | 对比度 -1 到 1 |
| brightness | `KFTypeBrightness` | VideoSegment | 亮度 -1 到 1 |
| volume | `KFTypeVolume` | AudioSegment/VideoSegment | 音量 1.0 为原始 |

**KeyframeList**（关键帧列表，挂在 segment.common_keyframes）：

| 字段 | 类型 | 标准值 | 说明 |
|---|---|---|---|
| id | string | `hexId()` | 列表全局 id |
| keyframe_list | Keyframe[] | `[...]` | 关键帧数组 |
| material_id | string | `""` | 素材 id（保留） |
| property_type | string | KeyframeProperty 值 | 属性类型 |

### 3.10 动画系统（Animation / SegmentAnimations）

来源 pyJianYingDraft `animation.py`：

**Animation**（单个动画）：

| 字段 | 类型 | 标准值 | 说明 |
|---|---|---|---|
| anim_adjust_params | null | `null` | 动画调节参数 |
| platform | string | `"all"` | 平台 |
| panel | string | `"video"` 或 `""` | 面板（视频动画="video"） |
| material_type | string | `"video"` 或 `"sticker"` | 素材类型 |
| name | string | 动画名称 | 动画名称 |
| id | string | effect_id | 动画效果 id |
| type | string | `"in"`/`"out"`/`"group"`/`"loop"` | 动画类型 |
| resource_id | string | resource_id | 资源 id |
| start | int | 微秒 | 相对片段开头的偏移 |
| duration | int | 微秒 | 动画持续时间 |

**动画类型**：
- **VideoAnimation**：`in`（入场）、`out`（出场）、`group`（组合）
- **Text_animation**：`in`（入场）、`out`（出场）、`loop`（循环）

**SegmentAnimations**（动画集合，挂在 segment.extra_material_refs）：

| 字段 | 类型 | 标准值 | 说明 |
|---|---|---|---|
| id | string | `hexId()` | 动画集合全局 id |
| type | string | `"sticker_animation"` | 类型固定 |
| multi_language_current | string | `"none"` | 多语言 |
| animations | Animation[] | `[...]` | 动画列表 |

### 3.11 render_index（段级）

按轨道顺序统一赋值：`draft_content_tracks_render_index(tracks)` —— 第 N 条轨的全部段 `seg.render_index = N`（0 起）。**唯一实现**，禁止逐段手写。源实现 `script_file.py` `dumps()` 在赋 `render_index` 的同时将全部段 `track_render_index` 重置为 `0`，且轨道按 `track_order` 排序导出（导入轨在前、新建轨在后）。

> **11.x 实测差异**：11.x 草稿中文本段的 render_index 值很大（如 14000、14014），而非简单的轨序 0/1/2。这是剪映内部的渲染层级编号，导出器仍按轨序赋值。

---

## 4. 素材（materials.*）

### 4.0 materials 类型全集（5.9 模板 45 种 + 11.x 新增 12 种 = 57 种）

| 类型 | 5.9 | 11.x | 说明 |
|---|---|---|---|
| ai_generate_holders | — | ✅ | **11.x 新增**，AI 生成占位 |
| ai_text_effects | — | ✅ | **11.x 新增**，AI 文本效果 |
| ai_translates | ✅ | ✅ | AI 翻译 |
| audio_balances | ✅ | ✅ | 音频平衡 |
| audio_effects | ✅ | ✅ | 音频特效 |
| audio_fades | ✅ | ✅ | 音频淡入淡出 |
| audio_pannings | — | ✅ | **11.x 新增**，音频声像 |
| audio_pitch_shifts | — | ✅ | **11.x 新增**，音频变调 |
| audio_track_indexes | ✅ | ✅ | 音频轨道索引 |
| audios | ✅ | ✅ | 音频素材 |
| beats | ✅ | ✅ | 节拍 |
| canvases | ✅ | ✅ | 画布（背景填充） |
| chromas | ✅ | ✅ | 色度抠图 |
| color_curves | ✅ | ✅ | 色彩曲线 |
| common_mask | — | ✅ | **11.x 新增**，通用蒙版（取代 masks） |
| digital_human_model_dressing | — | ✅ | **11.x 新增**，数字人换装 |
| digital_humans | ✅ | ✅ | 数字人 |
| drafts | ✅ | ✅ | 草稿 |
| effects | ✅ | ✅ | 效果（滤镜/花字/气泡/混合模式） |
| flowers | ✅ | ✅ | 花字 |
| green_screens | ✅ | ✅ | 绿幕 |
| handwrites | ✅ | ✅ | 手写 |
| hsl | ✅ | ✅ | HSL 调色 |
| hsl_curves | — | ✅ | **11.x 新增**，HSL 曲线 |
| images | ✅ | ✅ | 图片 |
| log_color_wheels | ✅ | ✅ | Log 色轮 |
| loudnesses | ✅ | ✅ | 响度 |
| manual_beautys | — | ✅ | **11.x 新增**，手动美颜 |
| manual_deformations | ✅ | ✅ | 手动变形 |
| masks | ✅ | — | 蒙版（11.x 被 common_mask 取代） |
| material_animations | ✅ | ✅ | 素材动画 |
| material_colors | ✅ | ✅ | 素材颜色 |
| multi_language_refs | ✅ | ✅ | 多语言引用 |
| placeholder_infos | — | ✅ | **11.x 新增**，占位信息 |
| placeholders | ✅ | ✅ | 占位 |
| plugin_effects | ✅ | ✅ | 插件效果 |
| primary_color_wheels | ✅ | ✅ | 主色轮 |
| realtime_denoises | ✅ | ✅ | 实时降噪 |
| shapes | ✅ | ✅ | 形状 |
| smart_crops | ✅ | ✅ | 智能裁剪 |
| smart_relights | ✅ | ✅ | 智能重光 |
| sound_channel_mappings | ✅ | ✅ | 声道映射 |
| speeds | ✅ | ✅ | 变速 |
| stickers | ✅ | ✅ | 贴纸 |
| tail_leaders | ✅ | ✅ | 尾引导 |
| text_templates | ✅ | ✅ | 文字模板 |
| texts | ✅ | ✅ | 文本 |
| time_marks | ✅ | ✅ | 时间标记 |
| transitions | ✅ | ✅ | 转场 |
| video_effects | ✅ | ✅ | 视频特效 |
| video_radius | — | ✅ | **11.x 新增**，视频圆角 |
| video_shadows | — | ✅ | **11.x 新增**，视频阴影 |
| video_strokes | — | ✅ | **11.x 新增**，视频描边 |
| video_trackings | ✅ | ✅ | 视频追踪 |
| videos | ✅ | ✅ | 视频素材 |
| vocal_beautifys | ✅ | ✅ | 人声美化 |
| vocal_separations | ✅ | ✅ | 人声分离 |

### 4.1 videos（视频素材）

来源 pyJianYingDraft `local_materials.py` VideoMaterial + 11.x 实测：

**pyJianYingDraft 基准字段**（17 个）：

| 字段 | 类型 | 标准值 | 说明 |
|---|---|---|---|
| audio_fade | null | `null` | 音频淡入淡出 |
| category_id | string | `""` | 分类 id |
| category_name | string | `"local"` | 分类名 |
| check_flag | int | `63487` | 检查标记 |
| crop | object | 四角全幅 | 裁剪设置（CropSettings，8 子字段见下表） |
| crop_ratio | string | `"free"` | 裁剪比例 |
| crop_scale | float | `1.0` | 裁剪缩放 |
| duration | int | 素材时长 us | 时长 |
| height | int | 像素 | 高度 |
| id | string | `hexId()` | 素材 id |
| local_material_id | string | `""` | 本地素材 id |
| material_id | string | =id | 素材 id（冗余） |
| material_name | string | 文件名 | 素材名 |
| media_path | string | `""` | 媒体路径 |
| path | string | 绝对路径 | 文件路径（正斜杠） |
| type | string | `"video"` / `"photo"` | 类型 |
| width | int | 像素 | 宽度 |

**crop（CropSettings）子字段**（来源 `local_materials.py` `CropSettings.export_json()`；取值 0-1，坐标原点在素材**左上角**，默认值即四角全幅 = 不裁剪）：

`upper_left_x`、`upper_left_y`、`upper_right_x`、`upper_right_y`、`lower_left_x`、`lower_left_y`、`lower_right_x`、`lower_right_y`

**11.x 实测新增字段**（20 个总字段 = 基准 17 个中保留 9 个 + 新增 11 个）：

| 字段 | 类型 | 实测值 | 说明 |
|---|---|---|---|
| beauty_face_auto_preset | object | `{}` | 美颜自动预设 |
| extra_type_option | int | `1` | 额外类型选项 |
| has_sound_separated | bool | `true` | 已分离声音 |
| is_copyright | bool | `true` | 有版权 |
| is_set_beauty_mode | bool | `true` | 设置美颜模式 |
| is_unified_beauty_mode | bool | `true` | 统一美颜模式 |
| matting | object | `{path:""}` | 抠像 |
| stable | object | `{time_range:{}}` | 稳定 |
| video_algorithm | object | `{path:"", story_video_modify_video_config:{}}` | 视频算法 |
| video_mask_shadow | object | `{resource_id:"", path:""}` | 视频遮罩阴影 |
| video_mask_stroke | object | `{resource_id:"", path:"", type:""}` | 视频遮罩描边 |

> **注意**：11.x 实测中 `crop` 为 `{}`（空对象），`material_id` 为 `""`，`material_name`/`media_path`/`local_material_id`/`audio_fade`/`category_id`/`category_name`/`crop_ratio`/`crop_scale` 字段被省略。导出器仍按完整字段输出。
> 另两条实测差异（2026-09-17）：① 11.x `check_flag` 实际值为 `62978047`（本标准仍输出 `63487`，与 5.9/pyJianYingDraft 一致）；② **剪映不按 path 去重素材**——同一文件可产生多个素材实例（实测同一 MP4 ×2 个 video 实例、同一 TTS wav ×11 个 audio 实例），而 pyJianYingDraft 模型是「一份素材多段复用」。导出器实例化策略待裁决（登记待做）。

### 4.2 audios（音频素材）

来源 pyJianYingDraft `local_materials.py` AudioMaterial + 11.x 实测：

**pyJianYingDraft 基准字段**（16 个）：

| 字段 | 类型 | 标准值 | 说明 |
|---|---|---|---|
| app_id | int | `0` | 应用 id |
| category_id | string | `""` | 分类 id |
| category_name | string | `"local"` | 分类名 |
| check_flag | int | `3` | 检查标记 |
| copyright_limit_type | string | `"none"` | 版权限制类型 |
| duration | int | 素材时长 us | 时长 |
| effect_id | string | `""` | 效果 id |
| formula_id | string | `""` | 公式 id |
| id | string | `hexId()` | 素材 id |
| local_material_id | string | =id | 本地素材 id |
| music_id | string | =id | 音乐 id |
| name | string | 文件名 | 素材名 |
| path | string | 绝对路径 | 文件路径 |
| source_platform | int | `0` | 来源平台 |
| type | string | `"extract_music"` | 类型 |
| wave_points | array | `[]` | 波形点 |

**11.x 实测新增字段**（22 个总字段 = 基准 16 个中保留 11 个 + 新增 11 个）：

| 字段 | 类型 | 实测值 | 说明 |
|---|---|---|---|
| music_source | string | `""` | 音乐来源 |
| request_id | string | `""` | 请求 id |
| resource_id | string | 剪映资源 id | 资源 id |
| similiar_music_info | object | `{}` | 相似音乐信息 |
| text_id | string | 文本素材 id | 关联文本（TTS 场景） |
| tone_effect_name | string | `"猴哥"` | 音色效果名 |
| tone_platform | string | `"sami"` | 音色平台 |
| tone_speaker | string | `"zh_male_sunwukong_clone2"` | 音色说话人 |
| tone_type | string | `"猴哥"` | 音色类型 |
| tts_benefit_info | object | `{benefit_type:"none"}` | TTS 权益信息 |
| tts_generate_scene | string | `"audio_panel"` | TTS 生成场景 |

> **11.x 实测 type 值集合**：`"text_to_audio"`（TTS 配音）、`"music"`（音乐库）、`"sound"`（音效）；`"extract_music"` 为 pyJianYingDraft/导出器写出的默认值（实测草稿中未出现）。
>
> **草稿内部素材路径占位符（11.x 实测，2026-09-17 定位）**：草稿目录**内部**的素材（如 TTS `textReading/*.wav`）的 `path` 不写绝对路径，而写
> `##_draftpath_placeholder_<UUID>_##/textReading/xxx.wav`
> （实测 11 处；解密工具 jy-draftc 无任何替换逻辑，确认为剪映自身的存储约定，运行时解析为当前草稿目录，因此草稿移动/改名后 TTS 仍可定位）。该 UUID 不在草稿目录任何明文文件中（绑定于 draft.extra 或内部库）。**§0.3 的「路径绝对化」必须识别并还原此前缀**；导出器若自产草稿内部素材（TTS 等），应遵循同一约定而非写绝对路径（登记待做）。

### 4.3 texts（文本素材）

来源 pyJianYingDraft `text_segment.py` TextSegment.export_material() + 11.x 实测：

**pyJianYingDraft 基准字段**（12 个无条件字段；设置背景时另合并 8 个条件字段，见下）：

| 字段 | 类型 | 标准值 | 说明 |
|---|---|---|---|
| id | string | `hexId()` | 素材 id |
| content | string | JSON 字符串 | 文本内容（结构见下） |
| typesetting | int | `0` | 排版（0=横排 1=竖排） |
| alignment | int | `0`=左 `1`=居中 `2`=右 | 对齐方式（字幕=1） |
| letter_spacing | float | `0` | 字符间距 |
| line_spacing | float | `0.02` | 行间距 |
| line_feed | int | `1` | 换行 |
| line_max_width | float | `0.82` | 最大行宽 |
| force_apply_line_max_width | bool | `false` | 强制应用最大行宽 |
| check_flag | int | `7` | 检查标记（+8=描边 +16=背景 +32=阴影） |
| type | string | `"text"` / `"subtitle"` | 类型 |
| global_alpha | float | `1.0` | 全局不透明度 |

**TextBackground 条件导出字段（8 个）**（来源 `text_segment.py` `TextBackground.export_json()`；**仅当文本设置背景时**合并进 texts 素材，check_flag 同时 +16）：

| 字段 | 类型 | 标准值 | 说明 |
|---|---|---|---|
| background_style | int | `1` | 背景样式（1/2 两种） |
| background_color | string | `"#RRGGBB"` | 背景颜色 |
| background_alpha | float | `1.0` | 背景不透明度 |
| background_round_radius | float | `0.0` | 背景圆角半径 |
| background_height | float | `0.14` | 背景高度 |
| background_width | float | `0.14` | 背景宽度 |
| background_horizontal_offset | float | `0.0` | 水平偏移（构造输入 0-1，导出映射 ×2−1） |
| background_vertical_offset | float | `0.0` | 竖直偏移（构造输入 0-1，导出映射 ×2−1） |

**11.x 实测新增字段**（38 个总字段 = 基准 12 个中保留 7 个 + 新增 31 个；`typesetting`/`line_feed`/`line_max_width`/`force_apply_line_max_width`/`global_alpha` 五个基准字段在 11.x 实测中因默认值被省略）：

| 字段 | 类型 | 实测值 | 说明 |
|---|---|---|---|
| add_type | int | `1` | 添加类型 |
| background_color | string | `"#000000"` | 背景颜色 |
| base_content | string | JSON 字符串 | 基础内容 |
| bold_width | float | — | 加粗宽度 |
| border_alpha | float | — | 描边不透明度 |
| border_color | string | `"#000000"` | 描边颜色 |
| border_width | float | `0.042...` | 描边宽度 |
| caption_template_info | object | `{resource_id:"", path:""}` | 字幕模板信息 |
| combo_info | object | `{}` | 组合信息 |
| current_words | object | `{}` | 当前词 |
| font_path | string | 字体文件路径 | 字体路径 |
| font_size | float | `10` | 字体大小 |
| fonts | array | — | 字体列表 |
| group_id | string | `"Auto_1789611642869"` | 组 id |
| initial_scale | float | `1` | 初始缩放 |
| italic_degree | float | — | 斜体角度 |
| layer_weight | int | `1` | 层权重 |
| lyrics_template | object | `{resource_id:"", path:""}` | 歌词模板 |
| name | string | — | 名称（**关键绑定键**，见 §5） |
| punc_model | string | `"llm"` | 标点模型 |
| recognize_model | string | `"paraformer"` | 识别模型 |
| recognize_task_id | string | `"local:..."` | 识别任务 id |
| recognize_text | string | 识别文本 | 识别结果 |
| shadow_alpha | float | `0.9` | 阴影不透明度 |
| shadow_angle | float | `-45` | 阴影角度 |
| shadow_distance | float | `5` | 阴影距离 |
| shadow_point | object | `{x, y}` | 阴影点 |
| shadow_smoothing | float | `0.45` | 阴影平滑 |
| subtitle_keywords | object | `{}` | 字幕关键词 |
| text_color | string | `"#ffffff"` | 文本颜色 |
| words | object | `{start_time:[], end_time:[], text:[]}` | 逐词时间戳 |

**content JSON 结构**（styles 数组 + text 字符串）：

```json
{
  "styles": [{
    "fill": {
      "alpha": 1.0,
      "content": {
        "render_type": "solid",
        "solid": { "alpha": 1.0, "color": [r, g, b] }
      }
    },
    "range": [0, 文本长度],
    "size": 8.0,
    "bold": false,
    "italic": false,
    "underline": false,
    "strokes": [{
      "content": { "solid": { "alpha": 1.0, "color": [r, g, b] } },
      "width": 0.04,
      "mode": 0
    }],
    "font": { "id": "字体资源id", "path": "字体路径" },
    "effectStyle": { "id": "花字效果id", "path": "C:" },
    "shadows": [{
      "diffuse": 0.025,
      "alpha": 1.0,
      "distance": 5.0,
      "content": { "solid": { "color": [r, g, b] } },
      "angle": -45.0
    }],
    "useLetterColor": true
  }],
  "text": "文本内容"
}
```

（`color` = 0-1 浮点三元组；`effectStyle` 仅花字/带效果文本有；`strokes` 仅描边文本有；`shadows` 仅阴影文本有；`font` 仅指定字体有。`strokes[].mode` 与 `useLetterColor` 为 **11.x 实测字段**——pyJianYingDraft 的 TextBorder **不导出** `mode`，导出器需自行补齐；`font.path` 在 pyJianYingDraft 中固定写占位值 `"D:"`，11.x 实测为真实字体路径；描边宽度映射 `width = 剪映值/100×0.2`；阴影扩散映射 `diffuse = 剪映值/100/6`。）

### 4.4 stickers（贴纸素材）

来源 pyJianYingDraft `video_segment.py` StickerSegment.export_material()：

| 字段 | 类型 | 标准值 | 说明 |
|---|---|---|---|
| id | string | `hexId()` | 素材 id |
| resource_id | string | 剪映资源 id | 资源 id |
| sticker_id | string | =resource_id | 贴纸 id |
| source_platform | int | `1` | 来源平台 |
| type | string | `"sticker"` | 类型 |

### 4.5 video_effects（视频特效素材）

来源 pyJianYingDraft `video_segment.py` VideoEffect：

| 字段 | 类型 | 标准值 | 说明 |
|---|---|---|---|
| adjust_params | array | `[EffectParamInstance…]` | 调节参数 |
| apply_target_type | int | `0`=片段 `2`=全局 | 应用目标类型 |
| apply_time_range | null | `null` | 应用时间范围 |
| category_id | string | `""` | 分类 id |
| category_name | string | `""` | 分类名 |
| common_keyframes | array | `[]` | 关键帧 |
| disable_effect_faces | array | `[]` | 禁用效果面 |
| effect_id | string | 剪映效果 id | 效果 id |
| formula_id | string | `""` | 公式 id |
| id | string | `hexId()` | 素材 id |
| name | string | 特效名 | 名称 |
| platform | string | `"all"` | 平台 |
| render_index | int | `11000` | 渲染索引 |
| resource_id | string | 剪映资源 id | 资源 id |
| source_platform | int | `0` | 来源平台 |
| time_range | null | `null` | 时间范围 |
| track_render_index | int | `0` | 轨道渲染索引 |
| type | string | `"video_effect"` / `"face_effect"` | 类型 |
| value | float | `1.0` | 值 |
| version | string | `""` | 版本 |

### 4.6 effects（效果素材：滤镜/花字/气泡/混合模式）

来源 pyJianYingDraft `video_segment.py` Filter / TextBubble / TextEffect / MixMode：

**滤镜（Filter）**：

| 字段 | 类型 | 标准值 | 说明 |
|---|---|---|---|
| adjust_params | array | `[]` | 调节参数 |
| algorithm_artifact_path | string | `""` | 算法工件路径 |
| apply_target_type | int | `0`=片段 `2`=全局 | 应用目标类型 |
| bloom_params | null | `null` | 泛光参数 |
| category_id | string | `""` | 分类 id |
| category_name | string | `""` | 分类名 |
| color_match_info | object | 三路径空对象 | 色彩匹配信息 |
| effect_id | string | 剪映效果 id | 效果 id |
| enable_skin_tone_correction | bool | `false` | 启用肤色校正 |
| exclusion_group | array | `[]` | 排除组 |
| face_adjust_params | array | `[]` | 面部调节参数 |
| formula_id | string | `""` | 公式 id |
| id | string | `hexId()` | 素材 id |
| intensity_key | string | `""` | 强度键 |
| multi_language_current | string | `""` | 当前多语言 |
| name | string | 滤镜名 | 名称 |
| panel_id | string | `""` | 面板 id |
| platform | string | `"all"` | 平台 |
| resource_id | string | 剪映资源 id | 资源 id |
| source_platform | int | `1` | 来源平台 |
| sub_type | string | `"none"` | 子类型 |
| time_range | null | `null` | 时间范围 |
| type | string | `"filter"` | 类型 |
| value | float | 强度 0-1 | 值 |
| version | string | `""` | 版本 |

**文本气泡（TextBubble）**：

| 字段 | 类型 | 标准值 | 说明 |
|---|---|---|---|
| apply_target_type | int | `0` | 应用目标类型 |
| effect_id | string | 剪映效果 id | 效果 id |
| id | string | `hexId()` | 素材 id |
| resource_id | string | 剪映资源 id | 资源 id |
| type | string | `"text_shape"` | 类型 |
| value | float | `1.0` | 值 |

**文本花字（TextEffect）**：继承 TextBubble，`type` 改为 `"text_effect"`，增加 `source_platform: 1`。

**混合模式（MixMode）**：

| 字段 | 类型 | 标准值 | 说明 |
|---|---|---|---|
| apply_target_type | int | `0` | 应用目标类型 |
| category_id | string | `""` | 分类 id |
| category_name | string | `""` | 分类名 |
| effect_id | string | 剪映效果 id | 效果 id |
| id | string | `hexId()` | 素材 id |
| name | string | 混合模式名 | 名称 |
| platform | string | `"all"` | 平台 |
| resource_id | string | 剪映资源 id | 资源 id |
| source_platform | int | `0` | 来源平台 |
| sub_type | string | `"none"` | 子类型 |
| time_range | null | `null` | 时间范围 |
| type | string | `"mix_mode"` | 类型 |
| value | float | `1.0` | 值 |

### 4.7 material_animations（素材动画）

来源 pyJianYingDraft `animation.py` SegmentAnimations + Animation：

**SegmentAnimations（容器）**：

| 字段 | 类型 | 标准值 | 说明 |
|---|---|---|---|
| id | string | `hexId()` | 动画组 id |
| type | string | `"sticker_animation"` | 类型 |
| multi_language_current | string | `"none"` | 当前多语言 |
| animations | array | `[Animation…]` | 动画列表 |

**Animation（单个动画）**：

| 字段 | 类型 | 标准值 | 说明 |
|---|---|---|---|
| anim_adjust_params | null | `null` | 动画调节参数 |
| platform | string | `"all"` | 平台 |
| panel | string | `"video"` / `""` | 面板（视频动画="video"，文本动画=""） |
| material_type | string | `"video"` / `"sticker"` | 素材类型 |
| name | string | 动画名 | 名称 |
| id | string | 剪映效果 id | 效果 id |
| type | string | `"in"` / `"out"` / `"group"` / `"loop"` | 动画类型 |
| resource_id | string | 剪映资源 id | 资源 id |
| start | int | 偏移 us | 相对段开头的偏移 |
| duration | int | 时长 us | 动画持续时间 |

### 4.8 speeds（变速素材）

来源 pyJianYingDraft `segment.py` Speed：

| 字段 | 类型 | 标准值 | 说明 |
|---|---|---|---|
| curve_speed | null | `null` | 曲线速度 |
| id | string | `hexId()` | 素材 id |
| mode | int | `0` | 模式 |
| speed | float | 速度值 | 播放速度 |
| type | string | `"speed"` | 类型 |

**每个媒体/文本段必须有一个对应 speed 素材**（`extra_material_refs` 引用）。

### 4.9 transitions（转场素材）

来源 pyJianYingDraft `video_segment.py` Transition + 11.x 实测：

| 字段 | 类型 | 标准值 | 说明 |
|---|---|---|---|
| category_id | string | `""` | 分类 id |
| category_name | string | `""` | 分类名 |
| duration | int | 转场时长 us | 持续时间 |
| effect_id | string | 剪映效果 id | 效果 id |
| id | string | `hexId()` | 素材 id |
| is_overlap | bool | 转场元数据 | 是否重叠 |
| name | string | 转场名 | 名称 |
| platform | string | `"all"` | 平台 |
| resource_id | string | 剪映资源 id | 资源 id |
| type | string | `"transition"` | 类型 |

**11.x 实测新增字段**：

| 字段 | 类型 | 实测值 | 说明 |
|---|---|---|---|
| path | string | 缓存路径 | 资源缓存路径 |
| request_id | string | 请求 id | 请求 id |
| source_platform | int | `1` | 来源平台 |
| third_resource_id | string | =resource_id | 第三方资源 id |

挂法：materials.transitions + **前一段** 的 `extra_material_refs`。

### 4.10 audio_fades（音频淡入淡出素材）

来源 pyJianYingDraft `segment.py` AudioFade：

| 字段 | 类型 | 标准值 | 说明 |
|---|---|---|---|
| id | string | `hexId()` | 素材 id |
| fade_in_duration | int | 淡入时长 us | 淡入 |
| fade_out_duration | int | 淡出时长 us | 淡出 |
| fade_type | int | `0` | 淡入淡出类型 |
| type | string | `"audio_fade"` | 类型 |

### 4.11 audio_effects（音频特效素材）

来源 pyJianYingDraft `audio_segment.py` AudioEffect：

| 字段 | 类型 | 标准值 | 说明 |
|---|---|---|---|
| audio_adjust_params | array | `[EffectParamInstance…]` | 音频调节参数 |
| id | string | `hexId()` | 素材 id |
| name | string | 音效名 | 名称 |
| resource_id | string | 剪映资源 id | 资源 id |
| type | string | `"audio_effect"` | 类型 |
| category_id | string | `"sound_effect"` / `"tone"` / `"speech_to_song"` | 分类 id |
| category_name | string | `"场景音"` / `"音色"` / `"声音成曲"` | 分类名 |
| is_ugc | bool | `false` | 是否 UGC |
| production_path | string | `""` | 生产路径 |
| speaker_id | string | `""` | 说话人 id |
| sub_type | int | `1` / `2` / `3` | 子类型 |
| time_range | object | `{duration:0, start:0}` | 时间范围 |

> **注意**：音色（ToneEffectType）类型的 AudioEffect 不导出 category_id/category_name/is_ugc/production_path/speaker_id/sub_type/time_range 字段。

### 4.12 canvases（画布/背景填充素材）

来源 pyJianYingDraft `video_segment.py` BackgroundFilling：

| 字段 | 类型 | 标准值 | 说明 |
|---|---|---|---|
| id | string | `hexId()` | 素材 id |
| type | string | `"canvas_blur"` / `"canvas_color"` | 填充类型 |
| blur | float | 模糊程度 0-1 | 模糊 |
| color | string | `"#RRGGBBAA"` | 背景颜色 |
| source_platform | int | `0` | 来源平台 |

### 4.13 chromas（色度抠图素材）

来源 pyJianYingDraft `video_segment.py` Chroma：

| 字段 | 类型 | 标准值 | 说明 |
|---|---|---|---|
| color | string | `"#RRGGBBAA"` | 抠图关键色 |
| edge_smooth_value | float | 0-1 | 边缘羽化 |
| id | string | UUID 大写 | 素材 id |
| intensity_value | float | 0-1 | 强度 |
| shadow_value | float | 0-1 | 阴影 |
| should_transfer_color | bool | `true` | 是否转移颜色 |
| spill_value | float | 0-1 | 边缘清晰 |
| type | string | `"chroma"` | 类型 |
| version | string | `"v2"` | 版本 |

### 4.14 masks（蒙版素材，5.9）/ common_mask（11.x）

来源 pyJianYingDraft `video_segment.py` Mask：

| 字段 | 类型 | 标准值 | 说明 |
|---|---|---|---|
| config | object | 蒙版配置 | 见下 |
| id | string | `hexId()` | 素材 id |
| name | string | 蒙版名 | 名称 |
| platform | string | `"all"` | 平台 |
| position_info | string | `""` | 位置信息 |
| resource_type | string | 资源类型 | 与蒙版形状相关 |
| resource_id | string | 剪映资源 id | 资源 id |
| type | string | `"mask"` | 类型 |

**config 子字段**：

| 字段 | 类型 | 说明 |
|---|---|---|
| aspectRatio | float | 宽高比 |
| centerX | float | 中心 X（半素材宽为单位） |
| centerY | float | 中心 Y（半素材高为单位） |
| feather | float | 羽化 0-1 |
| height | float | 高度 |
| invert | bool | 反转 |
| rotation | float | 旋转角度 |
| roundCorner | float | 圆角 0-1 |
| width | float | 宽度 |

### 4.15 keyframes（关键帧）

来源 pyJianYingDraft `keyframe.py` Keyframe + KeyframeList：

**Keyframe（单个关键帧）**：

| 字段 | 类型 | 标准值 | 说明 |
|---|---|---|---|
| curveType | string | `"Line"` | 曲线类型 |
| graphID | string | `""` | 图 id |
| left_control | object | `{x:0, y:0}` | 左控制点 |
| right_control | object | `{x:0, y:0}` | 右控制点 |
| id | string | `hexId()` | 关键帧 id |
| time_offset | int | 偏移 us | 相对素材起始点的时间偏移 |
| values | array | `[value]` | 关键帧值 |

**KeyframeList（关键帧列表）**：

| 字段 | 类型 | 标准值 | 说明 |
|---|---|---|---|
| id | string | `hexId()` | 列表 id |
| keyframe_list | array | `[Keyframe…]` | 关键帧列表 |
| material_id | string | `""` | 素材 id |
| property_type | string | 见下 | 属性类型 |

**KeyframeProperty 枚举**：

| 属性 | 值 | 说明 |
|---|---|---|
| position_x | `"KFTypePositionX"` | 水平位置（单位=半画布宽） |
| position_y | `"KFTypePositionY"` | 垂直位置（单位=半画布高） |
| rotation | `"KFTypeRotation"` | 旋转角度 |
| scale_x | `"KFTypeScaleX"` | X 轴缩放 |
| scale_y | `"KFTypeScaleY"` | Y 轴缩放 |
| uniform_scale | `"UNIFORM_SCALE"` | 统一缩放 |
| alpha | `"KFTypeAlpha"` | 不透明度 |
| saturation | `"KFTypeSaturation"` | 饱和度 -1~1 |
| contrast | `"KFTypeContrast"` | 对比度 -1~1 |
| brightness | `"KFTypeBrightness"` | 亮度 -1~1 |
| volume | `"KFTypeVolume"` | 音量 |

### 4.16 text_templates（文字模板素材）

来源 11.x 实测（pyJianYingDraft 未定义，由导出器 `buildTemplateClipTrio` 生成）：

| 字段 | 类型 | 标准值 | 说明 |
|---|---|---|---|
| id | string | `hexId()` | 素材 id |
| version | string | `"1.0.0"` | 版本 |
| effect_id | string | 剪映效果 id | 效果 id |
| resource_id | string | 剪映资源 id | 资源 id |
| name | string | 模板名 | 名称 |
| type | string | `"text_template"` | 类型 |
| path | string | artistEffect 缓存路径 | 路径 |
| category_id | string | 分类 id | 分类 id |
| category_name | string | 分类名 | 分类名 |
| source_platform | int | `1` | 来源平台 |
| resources | array | 资源列表 | 字体/文本/花字资源 |
| text_info_resources | array | 文本信息资源 | 文本 attach_info |
| non_text_info_resources | array | 非文本信息资源 | 贴纸 attach_info |
| aigc_config | object | AIGC 配置 | 字体项 |
| request_id | string | 请求 id | 请求 id |
| origin_word_info | object | `{}` | 原始词信息 |
| current_word_info | object | `{}` | 当前词信息 |
| preview_time | float | `0.1` | 预览时间 |
| ai_generate_task_info | object | `{resource_id:"", path:""}` | AI 生成任务信息 |

### 4.17 其他素材类型（11.x 实测有实例）

| 类型 | 字段 | 说明 |
|---|---|---|
| beats | `id`, `type:"beats"`, `ai_beats:{melody_percents:[]}` | 节拍 |
| placeholder_infos | `id`, `type:"placeholder_info"`, `meta_type:"none"` | 占位信息 |
| sound_channel_mappings | `id`, `type:"none"` | 声道映射 |
| material_colors | `id` | 素材颜色 |
| loudnesses | `id` | 响度 |
| vocal_separations | `id`, `type:"vocal_separation"` | 人声分离 |

---

## 5. 文字模板三件套（剪映原生模板引用式）

面向「零渲染保真」：由 `buildTemplateClipTrio(preset, phrase)` 从本机 `.textpreset` 机械重排生成（结构范本：王晗雨解密草稿同构实证）：

1. `materials.text_templates[]` ← preset.effect/resources/paragraphs[0].attach_info/elements（sticker）——字段清单见 §4.16。
2. `materials.texts[]` ← preset.paragraphs[0].content（仅替换 text + 对齐 range）。**关键绑定键：`texts.name = 预设 text_name`**（随机 id 时剪映回退渲染模板默认文字，2026-09-15 真机定位）。
3. 文字轨 segment ← `material_id` 指向 text_templates 实例 id；`extra_material_refs = [speed, 花字效果, 动画素材]`（按范本顺序）。

模板缺失（preset 不存在/结构不全）→ 跳过不造假；模板轨失败不阻断导出（字幕轨仍在）。

---

## 6. 内容时长与窗口口径

- `content.duration` = 各任务窗口累计 + 片段间半秒间隔（`VIDEO_GAP_US=500000`，最后一个不加）。
- **全轨窗口口径**（2026-09-16 用户实测定稿）：所有轨道按「任务窗口」定位，窗口 = 前序任务 content.duration 累计 + 0.5s 间隔。字幕 cue/模板/花字/音效为任务内局部系 + 窗口起点偏移。
- 有口播的素材段 `volume=0`（配音替换原声）；无口播保留原声。

---

## 7. 合规校验（导出「成功」的硬判据）

`verifyDraftFolder({draftFolder, expectedAssetCount})`：

| # | 检查项 | 不通过后果 |
|---|---|---|
| ① | `draft_content.json` / `draft_meta_info.json` 可解析 | 明确报错 |
| ② | 轨道非空 | 明确报错 |
| ③ | 所有素材 `path` 磁盘存在 | 明确报错（最多列 10 条） |
| ④ | assets 文件数 ≥ 清单数（expectedAssetCount>0 时） | 明确报错 |
| ⑤ | **段 `material_id` 无悬空**（必须在 materials.* 中存在，2026-09-16 新增） | 明确报错（`dangling` 计数） |

返回：`{ ok, trackCounts, pathRefs, missing, dangling, assetFiles, problems }`。不通过 → 导出 `success:false` 并明列问题。

---

## 8. 实现映射表（标准 ↔ 代码）

| 标准条目 | 实现（desktop/main/） | pyJianYingDraft 源码 | 单测钉住（tests/jianying-exporter.test.mjs） |
|---|---|---|---|
| §2 轨道 | `newTrack(type)` | `track.py` Track.export_json() | track 字段断言 |
| §2.1 TrackType 枚举 | — | `track.py` TrackType | — |
| §3.1 段基类 | `baseSegmentFields` | `segment.py` BaseSegment.export_json() | 字段断言 |
| §3.2 媒体段 | `mediaSegmentFields` | `segment.py` MediaSegment.export_json() | 字段断言 |
| §3.3 视觉段 | `visualSegmentFields` | `segment.py` VisualSegment.export_json() | 字段断言 |
| §3.3 ClipSettings | — | `segment.py` ClipSettings | — |
| §3.4 视频段 | — | `video_segment.py` VideoSegment | — |
| §3.5 音频段 | — | `audio_segment.py` AudioSegment | — |
| §3.6 文本段 | `buildSubtitleSegment`（**唯一构造器**，两路径共用） | `text_segment.py` TextSegment | 字幕段结构断言 |
| §3.7 贴纸段 | — | `video_segment.py` StickerSegment | — |
| §3.8 特效段/滤镜段 | — | `effect_segment.py` EffectSegment/FilterSegment | — |
| §3.9 关键帧 | — | `keyframe.py` Keyframe/KeyframeList | — |
| §3.10 动画 | — | `animation.py` Animation/SegmentAnimations | — |
| §3.11 render_index | `draft_content_tracks_render_index` | `script_file.py` dumps() | — |
| §4.1 视频素材 | `videoMaterialFields` | `local_materials.py` VideoMaterial.export_json() | 素材字段断言 |
| §4.2 音频素材 | `audioMaterialFields` | `local_materials.py` AudioMaterial.export_json() | 素材字段断言 |
| §4.3 文本素材 | `textMaterial` | `text_segment.py` TextSegment.export_material() | 素材字段断言 |
| §4.3 TextStyle | — | `text_segment.py` TextStyle | — |
| §4.3 TextBorder | — | `text_segment.py` TextBorder | — |
| §4.3 TextBackground | — | `text_segment.py` TextBackground | — |
| §4.3 TextShadow | — | `text_segment.py` TextShadow | — |
| §4.4 贴纸素材 | `stickerMaterial` | `video_segment.py` StickerSegment.export_material() | 素材字段断言 |
| §4.5 视频特效 | `videoEffectMaterial` | `video_segment.py` VideoEffect.export_json() | 素材字段断言 |
| §4.6 滤镜 | — | `video_segment.py` Filter.export_json() | — |
| §4.6 花字/气泡 | `textEffectMaterial` / `templateFlowerEffectMaterial` | `text_segment.py` TextBubble/TextEffect | 素材字段断言 |
| §4.6 混合模式 | — | `video_segment.py` MixMode.export_json() | — |
| §4.7 动画素材 | `textIntroAnimationMaterial` / `templateAnimMaterial` | `animation.py` SegmentAnimations/Animation | 素材字段断言 |
| §4.8 变速素材 | `speedMaterial` | `segment.py` Speed.export_json() | 素材字段断言 |
| §4.9 转场素材 | `buildTransitionMaterial` | `video_segment.py` Transition.export_json() | 素材字段断言 |
| §4.10 淡入淡出 | — | `segment.py` AudioFade.export_json() | — |
| §4.11 音频特效 | — | `audio_segment.py` AudioEffect.export_json() | — |
| §4.12 背景填充 | — | `video_segment.py` BackgroundFilling.export_json() | — |
| §4.13 色度抠图 | — | `video_segment.py` Chroma.export_json() | — |
| §4.14 蒙版 | — | `video_segment.py` Mask.export_json() | — |
| §4.15 关键帧 | — | `keyframe.py` Keyframe/KeyframeList | — |
| §4.16 文字模板 | `buildTemplateClipTrio` / `appendTextTemplateSegments` | — | 三件套断言 |
| §5 三件套 | `buildTemplateClipTrio` / `appendTextTemplateSegments` | — | 三件套断言 |
| §6 窗口口径 | `mergeJianyingManifests` / `montage-final-ipc` step 4 | — | 三轨同窗口回归 |
| §7 校验 | `verifyDraftFolder` | — | 完好/缺失/坏 JSON/悬空 四情形 |
| §0.3 条3 服务端段符合性上报 | `auditDraftStandardConformance` | — | 标准构造产物 0 警告 + 服务端旧格式特征（大写 id/duration=0/视频段 hdr_settings=null/标准外字段）全上报 |
| 附录 C 模板模式 | — | `template_mode.py` ImportedTrack/ImportedSegment | — |
| 附录 D 元数据 | — | `metadata/effect_meta.py` EffectMeta/EffectParam | — |
| 附录 E 工具类 | — | `time_util.py` Timerange / `draft_folder.py` DraftFolder | — |

---

## 9. 变更流程

1. 改标准（本文）→ 2. 改实现（映射表对应函数）→ 3. 单测钉住 → 4. 登记《剪映互通整体方案》附录 H 追溯表 → 5. 门禁（node --test + vue-tsc + vite build）。
**禁止**：绕过标准手写段对象/素材；克隆其他草稿当模板（封面事故教训，2026-09-16）。

---

## 附录 A：剪映 11.x 实测草稿文件清单

来源：`C:\Users\Administrator\AppData\Local\JianyingPro\User Data\Projects\com.lveditor.draft\9月17日-电商模板`

### 顶层文件

| 文件 | 大小 | 状态 | 说明 |
|---|---|---|---|
| draft_content.json | 219044 | 加密（V2） | 轨道/片段/素材主数据 |
| draft_content.json.bak | 219044 | 加密（V2） | 备份 |
| template-2.tmp | 219044 | 加密（V2） | 同 draft_content |
| draft_meta_info.json | 21024 | 加密（V2） | 元信息 |
| template.tmp | 3984 | 明文 | 11.x 空白草稿骨架 |
| key_value.json | 95919 | 明文 | 素材分类体系 |
| draft_agency_config.json | 22661 | 明文 | 代理素材配置 |
| draft_agency_info.json | 873 | 明文 | 素材分辨率映射 |
| draft_biz_config.json | 276 | 明文 | 时间线设置 |
| draft_virtual_store.json | 1226 | 明文 | 虚拟素材库 |
| timeline_layout.json | 206 | 明文 | 时间线布局 |
| attachment_editing.json | 2127 | 明文 | 编辑状态 |
| attachment_pc_common.json | 575 | 明文 | PC 通用 |
| performance_opt_info.json | 71 | 明文 | 性能优化 |
| draft_settings | 234 | 明文 INI | 设置 |
| draft_cover.jpg | 115383 | 二进制 | 封面 |
| draft.extra | 23932 | 二进制 | 额外数据 |
| agent_adoption_ledger.db | 106496 | SQLite | AI 采用账本 |

### 子目录

| 目录 | 说明 |
|---|---|
| adjust_mask/ | 调节蒙版 |
| common_attachment/ | 通用附件（6 个 JSON） |
| deepagent/ | AI 代理 |
| materials/ | 素材 |
| matting/ | 抠像 |
| Resources/ | 资源 |
| subdraft/ | 子草稿 |
| textReading/ | 文本朗读（TTS wav） |
| Timelines/ | 时间线（project.json + 时间线子目录） |

## 附录 B：key_value.json 素材分类体系

来源：`key_value.json`（110 个键，8 种 Category/Subcategory 组合）

| materialCategory | materialSubcategory | 数量 | 说明 |
|---|---|---|---|
| media | local | 68 | 本地视频/图片 |
| audio | music | 2 | 音乐 |
| audio | sound_effect | 17 | 音效 |
| text | text_template | 14 | 文字模板 |
| trans | transition_effect | 3 | 转场 |
| media | media_lib | 1 | 媒体库素材 |
| tone | audio_effects | 1 | 音色 |
| sticker | sticker | 2 | 贴纸 |

**通用字段**（所有素材共有）：`materialCategory`, `materialId`, `materialName`, `materialSubcategory`, `materialSubcategoryId`, `materialThirdcategory`, `materialThirdcategoryId`, `is_vip`, `rank`, `rec_id`, `requestId`, `textTemplateVersion`, `material_copyright`, `material_is_purchased`, `is_favorite`, `is_from_artist_shop`, `is_limited`, `is_similar_music`, `douyin_music_is_avaliable`, `music_source`, `original_song_id`, `original_song_name`, `pgc_id`, `pgc_name`, `previewed`, `previewed_before_added`, `special_effect_loading_type`

**扩展字段**（部分素材有）：`commerce_template_cate`, `commerce_template_pay_status`, `commerce_template_pay_type`, `enter_from`, `filter_category`, `filter_detail`, `is_brand`, `keywordSource`, `role`, `searchId`, `searchKeyword`, `team_id`, `template_author_id`, `template_drafts_price`, `template_duration`, `template_fragment_cnt`, `template_need_purcahse`, `template_pay_type`, `template_type`, `template_use_cnt`, `segmentId`

**特殊键**：`kLocalAgentSessionInfoKey` / `kPendingLocalAgentSessionInfoKey`（AI 会话 `conversation_id_list`）

---

## 附录 C：模板模式（Template Mode）

来源 pyJianYingDraft `template_mode.py`，用于**导入现有草稿并替换素材**的场景。

### C.1 核心概念

| 概念 | 说明 |
|---|---|
| **模板模式** | 加载现有草稿作为模板，替换其中的素材内容 |
| **非模板模式** | 从零创建新草稿（本文档主要场景） |

### C.2 导入轨道类型（ImportedTrack 继承体系）

| 类 | 说明 | 允许修改 |
|---|---|---|
| `ImportedTrack` | 基础导入轨道（只读） | ❌ |
| `EditableTrack` | 可编辑轨道基类 | ✅ |
| `ImportedTextTrack` | 导入的文本轨道 | ✅ |
| `ImportedMediaTrack` | 导入的音频/视频轨道 | ✅ |

**轨道类型与允许修改对照**（TrackType.allow_modify）：

| TrackType | 允许修改 | 说明 |
|---|---|---|
| video | ✅ | 视频轨 |
| audio | ✅ | 音频轨 |
| text | ✅ | 文本轨 |
| sticker | ❌ | 贴纸轨（只读） |
| effect | ❌ | 特效轨（只读） |
| filter | ❌ | 滤镜轨（只读） |
| adjust | ❌ | 调节轨（仅供导入，不要新建） |

### C.3 导入片段类型（ImportedSegment 继承体系）

| 类 | 说明 |
|---|---|
| `ImportedSegment` | 基础导入片段 |
| `ImportedMediaSegment` | 导入的媒体片段（含 source_timerange） |

### C.4 素材替换模式

**ShrinkMode**（素材变短时的处理）：

| 模式 | 说明 |
|---|---|
| `cut_head` | 裁剪头部，后移片段起始点 |
| `cut_tail` | 裁剪尾部，前移片段终止点 |
| `cut_tail_align` | 裁剪尾部并消除间隙，后续片段依次前移 |
| `shrink` | 保持中间点不变，两端点向中间靠拢 |

**ExtendMode**（素材变长时的处理）：

| 模式 | 说明 |
|---|---|
| `cut_material_tail` | 裁剪素材尾部，保持片段原长（总是成功） |
| `extend_head` | 延伸头部，与前续片段重合时失败 |
| `extend_tail` | 延伸尾部，与后续片段重合时失败 |
| `push_tail` | 延伸尾部，必要时后移后续片段（总是成功） |

### C.5 模板模式接口（对源核验 2026-09-17）

| 方法 | 说明 |
|---|---|
| `DraftFolder.load_template(draft_name)` | 按草稿名加载模板草稿（ScriptFile 侧为私有 `_load_template(json_path)`） |
| `DraftFolder.duplicate_as_template(template_name, new_draft_name)` | 复制草稿并以模板模式打开副本 |
| `ScriptFile.inspect_material()` | 输出贴纸/文字气泡/花字素材元数据 |
| `ScriptFile.replace_material_by_name(material_name, material, replace_crop=False)` | 按素材名替换（不改片段时长，适合图片） |
| `ScriptFile.replace_material_by_seg(track, segment_index, material, source_timerange=None, *, handle_shrink, handle_extend)` | 按片段替换（走 ShrinkMode/ExtendMode，见 C.4） |
| `ScriptFile.replace_text(track, segment_index, text, recalc_style=True)` | 替换文本轨道内容（普通文本/文字模板，自动重算样式 range） |
| `ScriptFile.get_imported_track(track_type, name=None, index=None)` | 获取唯一匹配的可编辑导入轨道 |
| `ScriptFile.list_imported_tracks(track_type=None)` | 列出导入轨道 |
| `ScriptFile.import_track(source_file, track, *, offset, new_name, under_track, over_track, at_index)` | 从另一草稿导入轨道（连带素材） |
| `ScriptFile.save()` | 保存到打开时的路径 |

---

## 附录 D：元数据类型（metadata）

来源 pyJianYingDraft `metadata/` 目录，定义特效/滤镜/转场/动画等的元数据结构。

### D.1 EffectMeta（特效元数据）

| 字段 | 类型 | 说明 |
|---|---|---|
| name | string | 效果名称 |
| is_vip | bool | 是否 VIP 特权 |
| resource_id | string | 资源 ID |
| effect_id | string | 效果 ID |
| md5 | string | MD5 校验 |
| params | EffectParam[] | 参数信息列表 |

### D.2 EffectParam（特效参数）

| 字段 | 类型 | 说明 |
|---|---|---|
| name | string | 参数名称 |
| default_value | float | 默认值 |
| min_value | float | 最小值 |
| max_value | float | 最大值 |

### D.3 EffectParamInstance（特效参数实例）

| 字段 | 类型 | 说明 |
|---|---|---|
| default_value | float | 默认值 |
| max_value | float | 最大值 |
| min_value | float | 最小值 |
| name | string | 参数名称 |
| parameterIndex | int | 参数索引 |
| portIndex | int | 端口索引（固定 0） |
| value | float | 当前值 |

### D.4 AnimationMeta（动画元数据）

| 字段 | 类型 | 说明 |
|---|---|---|
| title | string | 动画名称 |
| is_vip | bool | 是否 VIP |
| duration | int | 默认时长（微秒） |
| resource_id | string | 资源 ID |
| effect_id | string | 效果 ID |
| md5 | string | MD5 校验 |

### D.5 MaskMeta（蒙版元数据）

| 字段 | 类型 | 说明 |
|---|---|---|
| name | string | 蒙版名称 |
| resource_type | string | 资源类型（与形状相关） |
| resource_id | string | 资源 ID |
| effect_id | string | 效果 ID |
| md5 | string | MD5 校验 |
| default_aspect_ratio | float | 默认宽高比 |

### D.6 TransitionMeta（转场元数据）

| 字段 | 类型 | 说明 |
|---|---|---|
| name | string | 转场名称 |
| is_vip | bool | 是否 VIP |
| resource_id | string | 资源 ID |
| effect_id | string | 效果 ID |
| md5 | string | MD5 校验 |
| default_duration | int | 默认时长（微秒） |
| is_overlap | bool | 是否允许重叠 |

---

## 附录 E：工具类（time_util / draft_folder）

### E.1 Timerange（时间范围）

| 字段 | 类型 | 说明 |
|---|---|---|
| start | int | 起始时间（微秒） |
| duration | int | 持续长度（微秒） |

**方法**：
- `end` → `start + duration`
- `overlaps(other)` → 判断是否与另一时间范围重叠
- `export_json()` → `{start, duration}`

**便捷函数**：
- `tim("1h52m3s")` → 微秒数
- `trange(start, duration)` → Timerange 对象
- `srt_tstamp("00:00:01,000")` → 微秒数

### E.2 DraftFolder（草稿目录管理）

| 方法 | 说明 |
|---|---|
| `create_draft(draft_name, width, height, fps=30, *, maintrack_adsorb=True, allow_replace=False)` | 创建新草稿 |
| `list_drafts()` | 列出所有草稿（仅列子目录名，不校验格式） |
| `has_draft(draft_name)` | 检查草稿是否存在 |
| `remove(draft_name)` | 删除草稿（整目录 rmtree） |
| `load_template(draft_name)` | 以模板模式打开草稿 |
| `duplicate_as_template(template_name, new_draft_name, allow_replace=False)` | 复制草稿并打开副本 |
| `inspect_material(draft_name)` | 输出草稿内贴纸/气泡/花字素材元数据 |

### E.3 draft_content_loader（加密草稿读取入口）

来源 `draft_content_loader.py`：`load_draft_content(json_path, fallback_loader)` 先按明文 JSON 读取，失败时调用 `FallbackLoader`（输入原始字节，返回 str 或 dict）。挂载方式：`DraftFolder(folder_path, fallback_loader=...)`。**与本项目的关联**：11.x 实测草稿 `draft_content.json` 为 V2 加密，pyJianYingDraft 读取它们的官方扩展点就是此接口（本项目对应实现为 jy-draftc 解密工具链）。`jianying_controller.py`（Windows UI 自动化导出，依赖 uiautomation）与本标准无关，不纳入。

---

**文档版本**：2026-09-17 修订版
**数据源**：pyJianYingDraft 0.3.0 全量源码 + 剪映 11.5.5 实测解密草稿 + key_value.json 素材分类体系
**变更记录**：
- 2026-09-17：基于 pyJianYingDraft 全量源码补全所有轨道类型定义，新增附录 C（模板模式）、附录 D（元数据类型）、附录 E（工具类）
- 2026-09-17（二次修订，pyJianYingDraft 0.3.0 对源核验 + 11.5.5 解密草稿复核）：修正计数错误 §4.0（45+**12**=**57**，漏算 ai_generate_holders）、§4.1（20 = 保留 9 + 新增 11）、§4.2（22 = 保留 11 + 新增 11）、§4.3（基准 **12** 无条件 + 新增 **31** = 38）；§4.3 补 TextBackground 8 条件字段、§4.1 补 CropSettings 8 子字段；§4.2 新增「草稿内部素材路径占位符 `##_draftpath_placeholder_##`」约定与 audios.type 实测集合；§4.1 补 check_flag 62978047 差异与「剪映不按 path 去重素材」事实；§3.2 补 volume 可大于 1（实测 6.0987）；§3.11 补 track_render_index 重置与 track_order 轨序；§4.3 content 注记补 mode/useLetterColor 为 11.x 字段（pyJianYingDraft 不导出 mode）；附录 C.5/E.2 修正方法名（`remove`/`load_template`/`replace_material_by_*` 等）并补全 API；新增 E.3 draft_content_loader；§1.6 行数 67→66；§1.2/§1.5 加 template.tmp 来源注记（该文件已被剪映清理，不可再现）。**导出器待办**：路径绝对化识别占位符前缀、草稿内部素材遵循占位符约定、素材实例化（去重）策略待裁决——三项均已登记待做。
- 2026-09-17（三次修订·对齐收尾，导出器 → 标准）：① 音频素材 `local_material_id`/`music_id` = id（§4.2）；② texts 补 `line_max_width:0.82` + content `useLetterColor:true`（§4.3）；③ 文本/贴纸段 `source_timerange=null`、贴纸段去 `hdr_settings`（§3.6/§3.7）；④ `draft_meta_info.json` 字段名对齐骨架（§1.6，并登记导出器最小 meta 形状）；⑤ 新增 `auditDraftStandardConformance` 符合性审计（§0.3 条3 落地，上报不阻断，接入两条导出路径回传 `conformance`）；⑥ §8 映射表补登审计行。单测 26/26 钉住；互通方案 §0.3 补「两条链路口径注记」、附录 H #18 登记。
- 2026-09-17（四次修订·golden 验收设施 + 存量 bug 修复）：新增 `desktop/scripts/audit-golden-draft.cjs`（导出草稿 vs 剪映真机草稿对照：轨道构成/画幅/总时长/符合性/合规五项门禁）；**首轮对照即抓出存量 bug**——`appendBgmTrack` 段 `material_id` 与音频素材各自生成 id → 段引用悬空（§7-⑤ 语义），已修复并单测钉住。审计警告改走 main.log（铁律 5），渲染层透出 conformance（防「回传字段无人消费」）。新增附录 F 待裁决清单。
- 2026-09-17（五次修订·架构裁决落地）：§0.3 条3 改写——**服务端只出素材+语义清单、不出 draft 结构**（用户裁决 B），draft 全部由客户端标准构造器生成；过渡期旧契约直传段维持「三件事+上报」。附录 F：F1–F4 销项（维持现状），F5–F8 立项 P2，F9 改判为契约改造 P1。同日用户复核补充：契约补 `events[]` 事件表（文字模板/花字命中结果+时机随清单下发，覆盖 §2.3 九类产出）+ 服务端清单自检五条款——命中判定在服务端，清单不承载则客户端重算即成另一个双实现。
- 2026-09-19（六次修订·字幕轨属性改判）：用户报障「生成的字幕轨道与口播轨没有区别，字幕应该是**有字幕属性的轨道**」。对源 §2.2/§2.4 11.x 实测：真机字幕轨 `flag=1`（其它轨 0），即剪映区分「字幕轨」与普通文本轨的轨道级标记（草稿根级 `subtitle_*` 四字段是智能字幕任务产物，非轨道属性）。**改判 F3**：导出器字幕轨（`newTrack('text', 1)`，jianying-exporter.js 唯一创建点）带 `flag=1`；花字/文字模板/音视频/BGM/音效/贴纸轨维持 0。单测钉住（字幕轨 flag=1 断言）。

---

## 附录 F：待裁决清单（2026-09-17 建立）

> 本清单汇总标准落地所需的悬而未决裁决；裁决后回写对应章节并销项（变更流程见 §9）。

**✅ 已裁决销项（2026-09-17 用户拍板）**：

| # | 裁决项 | 裁决结果 |
|---|---|---|
| F1 | 素材实例化（去重）策略 | **维持现状**：一份素材多段复用（pyJianYingDraft 口径），不跟随剪映多实例 |
| F2 | 草稿内部素材路径约定 | **维持现状**：资产复制进 `assets/` + 绝对路径；不破解/不跟随 `##_draftpath_placeholder_##` 占位符 |
| F3 | 字幕轨 `flag` | ~~维持 `flag=0`~~ → **2026-09-19 改判 `flag=1`**（用户报障：生成的字幕轨与口播/普通轨无区别；11.x 实测 `flag=1` 即剪映「字幕轨」属性标记，见 §2.2/§2.4。导出器字幕轨已带 flag=1，花字/模板/音视频轨仍 0） |
| F4 | volume 放大上限 | **维持现状**（BGM/音效 ≤1；剪映实测 6.0987 为其 TTS 响度归一化产物，非我方输出场景） |

**🔧 已裁决立项（2026-09-17 用户拍板要做）**：

| # | 裁决项 | 裁决结果 | 落地形态 |
|---|---|---|---|
| F5–F7 | 模板保真（智能字幕逐词时间戳 / 口播 text_to_audio / 口播切片模型） | **追平再编辑能力** | 立项 P2（服务端依赖，见互通方案 §3.6） |
| F8 | BGM/音效资源注册 | 随 P2 一并调研（本地库音频源自剪映缓存同步，可回带 resource_id） | 立项 P2 调研项 |
| F9 | 服务端 from-task 输出 | **契约改为「素材+语义清单」**（2026-09-17 裁决 B），不再要求服务端出标准 draft | 立项 P1（互通方案 §3.6，契约草案在案） |
